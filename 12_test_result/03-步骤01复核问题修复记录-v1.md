# 步骤 01 复核问题修复记录 v1

## 1. 修复范围

本次修复对应步骤 01 复核中剩余的两个问题：

- 前端 `httpClient.ts` 丢失后端结构化错误语义。
- 前端骨架视觉过度装饰，不符合管理台骨架定位。

## 2. 已完成修复

### 2.1 结构化错误保留

已调整文件：

- `11_code/frontend/src/api/httpClient.ts`

修复内容：

- `RawApiError` 已补齐 `code` 和 `details`。
- 新增仅传输层使用的 `HttpRequestError`。
- `request<T>()` 在 `!response.ok` 或 `!payload.success` 时抛出结构化错误，保留：
  - `httpStatus`
  - `code`
  - `message`
  - `details`
- 未新增长期维护的前端业务 DTO；临时解析结构仍只存在于传输层文件内部。

### 2.2 管理台骨架视觉收敛

已调整文件：

- `11_code/frontend/src/styles/global.css`
- `11_code/frontend/src/shared/components/FeatureIntroCard.tsx`
- `11_code/frontend/src/features/home/pages/HomePage.tsx`

修复内容：

- 根背景改为中性色纯色背景，不再使用大面积径向渐变。
- 卡片圆角统一收敛到 `8px`。
- 删除卡片套卡片结构，`FeatureIntroCard` 改为简单列表。
- 首页去掉 hero 式展示块，改为普通标题区 + 指标区。

## 3. 验证结果

- `npm run build`
  - 结果：通过。

## 4. 结论

本轮复核要求的两个剩余问题已完成收口，步骤 01 的前端骨架更接近后台管理台基础形态，同时保留了后续步骤所需的结构化错误信息。
