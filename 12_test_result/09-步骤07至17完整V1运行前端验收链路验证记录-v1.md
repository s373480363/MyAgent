# 步骤 07 至 17 完整 V1 运行、前端与验收链路验证记录 v1

## 1. 验证范围

本记录对应 `8_change_list/agent-management-platform-v1/steps.md` 中步骤 07 至步骤 17 的连续实现收口。

重点验证范围：

- 运行内核基础组件
- V1 节点执行器集合
- 运行查询与 Trace 详情
- 前端主数据、Agent、Workflow、Runs、Evals 页面构建
- 节点验收后端链路

## 2. 自动化验证结果

- 后端 `mvn -q test` 通过。
- 后端测试结果：`Tests run: 52, Failures: 0, Errors: 0, Skipped: 2`。
- 前端 `npm run build` 通过。
- 前端构建存在 Vite chunk 体积提示，但未影响编译产物生成。

## 3. 新增测试覆盖

- `NodeExecutorRuntimeSemanticsTests`
  - `EXTERNAL_AGENT` 在敏感 header 未配置 secret 时必须在真正调用前失败。
  - `EXTERNAL_AGENT` 未提取到结构化 `outputJson` 时会退化为文本摘要作为业务输出。
  - `AGENT_CALL` 会创建子 `AgentRun`，同步调用目标运行引擎，并写入 `AgentMessage`。

## 4. 已覆盖的关键回归点

- `mvn test` 覆盖 OpenAPI 契约测试、运行时单元测试、工作流版本语义测试、设置白名单测试、外部 Agent secret 更新语义测试和 Eval 断言测试。
- `npm run build` 覆盖 TypeScript 类型检查、路由页面编译和当前 OpenAPI 生成类型消费链路。

## 5. 环境限制

- 当前机器 Docker 不可用，`PostgresMigrationTests` 相关 Testcontainers 用例跳过 2 个。
- 未在本地执行真实 OpenAI 网络调用；模型网关代码路径已接入 Spring AI `OpenAiChatModel`，真实调用需在具备 API Key 的运行环境联调。

## 6. 验证结论

步骤 07 至 17 的当前工程实现已通过本地自动化验证，可以提交架构师进行完整 V1 复核。
