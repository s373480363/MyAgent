# 步骤 17 测试联调与发布准备收口验证记录 v1

## 1. 验证范围

本记录用于收口 `10_plan_process/02-V1开发执行步骤拆解-v1.md` 中步骤 17 的开发侧验证结果，覆盖：

- 前端单元测试与页面关键链路测试。
- 前端生产构建。
- 后端 Maven 回归测试。
- 已知环境限制与发布前补验项。

## 2. 前端单元测试

执行目录：

```powershell
D:\myproject\MyAgent\11_code\frontend
```

执行命令：

```powershell
npm test -- --run
```

执行结果：

- 退出码：0。
- Test Files：7 passed。
- Tests：11 passed。
- 覆盖内容包括 Workflow、Debug、Runs、Evals、Settings、JsonBlock 和 API 路径契约。

非阻塞提示：

- jsdom 输出 `Window's getComputedStyle() method: with pseudo-elements` 未实现提示。
- 原因是 Ant Design 在测试环境触发 pseudo-elements 样式能力，当前断言全部通过，不影响步骤 17 验收。

## 3. 前端生产构建

执行目录：

```powershell
D:\myproject\MyAgent\11_code\frontend
```

执行命令：

```powershell
npm run build
```

执行结果：

- 退出码：0。
- TypeScript build 通过。
- Vite production build 通过。
- 输出文件包含：
  - `dist/index.html`
  - `dist/assets/index-DHr-NLDZ.css`
  - `dist/assets/index-B2cXWaGS.js`

非阻塞提示：

- Vite 提示主 chunk 超过 500 kB。
- 当前原因是 Ant Design、React Flow 与管理台页面集中打包；已作为发布后体验优化项记录到 `13_release/03-V1遗留问题清单-v1.md`。

## 4. 后端回归测试

执行目录：

```powershell
D:\myproject\MyAgent\11_code\backend
```

执行命令：

```powershell
$env:JAVA_HOME='D:\myproject\MyAgent\.tools\jdk21\jdk-21.0.11+10'
$env:Path='D:\myproject\MyAgent\.tools\jdk21\jdk-21.0.11+10\bin;D:\myproject\MyAgent\.tools\maven-3.9.11\apache-maven-3.9.11\bin;' + $env:Path
mvn -q test
```

执行结果：

- 退出码：0。
- OpenAPI 契约测试、应用上下文测试、领域与运行链路相关测试通过。
- PostgreSQL Testcontainers 已实际启动 `postgres:16-alpine`，Flyway 成功应用 `V1__create_core_tables.sql` 并验证 schema 版本为 `v1`。
- Maven 使用 JDK 21 和 Maven 3.9.11 执行。

非阻塞提示：

- Mockito 输出动态 Java Agent 加载提示，不影响本轮测试结果。

## 5. 本轮修复确认

- `WorkflowPage` 保存草稿时会合并原版本 `runtimeOptions` 与表单值，避免未打开运行约束 Tab 时提交空对象。
- `RunsPage` 的运行详情测试固定验证 `nodeRunId` 使用 `node_run.id`，不派生第二套 NodeRun 编号。
- `domainApi.test.ts` 覆盖关键 REST 路径，防止前端 API wrapper 偏离正式接口契约。
- 发布前检查清单、部署说明、遗留问题清单已输出到 `13_release`。

## 6. 架构审查修复复核

针对运行契约、Trace 契约和调试可信度审查意见，已完成以下复核：

- `DefaultWorkflowRuntimeEngine` 已接入运行总超时和节点级超时，阻塞节点执行会按运行剩余时间与节点超时的较小值触发 `RUN_TIMEOUT/TIMEOUT`。
- Schema 输入/输出校验会写入 `SCHEMA_VALIDATION` Trace，并把聚合结果落到 `NodeRun.schemaValidationResultJson`；同步运行错误保留字段级 `details`。
- LLM/REVIEW/SUMMARY 统一读取 `userPromptTemplate/systemPromptTemplate`，发布校验按字段存在拒绝旧 `prompt/promptTemplate`。
- CONDITION 条件求值错误按 FAIL_FAST 失败，不再用默认分支掩盖缺路径、非法结构、类型不匹配或不支持操作符。
- `agent_run.error_code` 已纳入 DDL、Record、Mapper、Repository 和运行详情返回，状态不再伪造成错误码。
- REVIEW/SUMMARY 对齐模型请求和响应 Trace；JAVA_METHOD、TOOL、EXTERNAL_AGENT Trace 保存结构化输入输出。
- 运行详情 `childRuns` 返回子运行 Agent、状态、开始/结束时间、耗时和摘要。
- OpenAI 客户端未配置时直接失败，不再走隐藏本地成功回退。
- 默认 `spring.flyway.enabled=true`，测试 profile 单独关闭 Flyway 以保持单元测试隔离。

## 7. 架构复核专项追加验证

针对普通运行、调试运行、AGENT_CALL 与 Eval 运行之间的语义一致性复核，已完成以下验证：

- Eval 执行目标节点时复用 `NodeExecutionRunner`，并通过 `nodeRunnerUsesResolvedInputWithoutApplyingInputMappingAgain` 验证 `EvalCase.inputJson` 不会再次应用目标节点 `inputMapping`。
- AGENT_CALL 父节点超时后，通过 `agentCallChildRunIsCanceledWhenParentNodeTimesOut` 验证活跃子 AgentRun 不会停留在 `RUNNING`，而是级联更新为 `CANCELED`。
- Trace 对外 DTO 已改为 `detail` 字段，前端 `RunsPage` 运行详情改读 `record.detail`；`evalRunId` 按 `eval_run.run_no` 字符串返回。
- REVIEW/SUMMARY 在 outputSchema 校验前写入 `MODEL_RESPONSE`，通过模型输出不符合 Schema 的用例验证 Trace 中仍保留模型原始响应。
- 新增 `DefaultEvalScoreEvaluatorTests`，覆盖空 `scoreRule` 不调用模型、非空 `scoreRule` 调用模型并返回评分结果、模型评分失败只写评分失败信息。
- `WorkflowNodeDefinition` 结构类 getter/setter 已补齐中文 Javadoc，避免新增触碰文件继续违反项目注释规范。

## 8. V1 整改专项验证

针对最终整改要求，已完成以下验证：

- `WorkflowJsonTypeHandlerTests` 覆盖 `WorkflowRuntimeOptions` 与 `ReferencedSchemaVersion` 列表从 JSON 文本稳定反序列化。
- `DefaultWorkflowRuntimeEngine` 已引入 LangGraph4j `StateGraph` 作为正式图调度路径，运行总超时和节点超时测试继续通过。
- `NodeExecutionRunner` 改用 Spring 管理的受限节点执行线程池，超时和 AGENT_CALL 级联取消测试继续通过。
- `DefaultJavaMethodRegistryTests` 覆盖已注册方法、停用方法、主数据存在但未显式注册方法。
- `DefaultToolRegistryTests` 覆盖已注册工具、停用工具、未知工具执行器类型。
- `NodeExecutorRuntimeSemanticsTests` 覆盖 JAVA_METHOD 通过 Registry/Invoker 执行、TOOL 通过 Registry/Executor 执行、REVIEW/SUMMARY 输入 Schema 失败不调用模型。

增量验证：

- 后端 `mvn -q test` 已重新通过。
- 前端 `npm test -- --run` 已通过。
- 前端 `npm run build` 已通过。
- `git diff --check` 退出码为 0；仅提示 Windows 行尾转换，不存在空白错误。

## 9. Docker + Poe 真实模型链路补验

本轮已在本机 Docker 环境完成真实部署形态补验：

- PostgreSQL 使用 Docker 容器 `postgres:16-alpine`，后端通过 Flyway 从空库执行 `V1__create_core_tables.sql`。
- 后端使用 Docker 容器运行 JDK 21 Jar，连接 Docker PostgreSQL，并通过 `SPRING_AI_OPENAI_BASE_URL=https://api.poe.com` 接入 Poe OpenAI-compatible API。
- Poe API Key 仅作为容器环境变量注入，未写入仓库、文档或测试结果。
- 端到端链路已完成：`Schema -> Agent -> Workflow Draft -> Validate -> Publish -> Published Run -> RunDetail`。
- 真实运行结果：`run_20260531205516_d30f894a`，`runStatus=SUCCESS`，`detailStatus=SUCCESS`。
- 运行输出为真实模型返回并经本地 JSON 解析与 Schema 校验后的结果：`{"ok":true,"summary":"MyAgent is running in Docker and using Poe as an OpenAI-compatible model gateway."}`。
- RunDetail 验证结果：`nodeRunCount=3`，节点状态为 `start:SUCCESS`、`llm:SUCCESS`、`end:SUCCESS`。
- Trace 验证结果：`traceEventCount=7`，包含 `MODEL_REQUEST`、`MODEL_RESPONSE` 和 4 条 `SCHEMA_VALIDATION`。

本轮补验中发现并修复 3 个会阻塞真实运行的工程问题：

- `SchemaMapper.xml` 构造器映射使用 `long/int/boolean` 时 MyBatis 按包装类型查找构造函数，已改为 `_long/_int/_boolean`。
- `trace_event.summary` 是 `varchar(500)`，异常消息直接作为 summary 会导致二次数据库异常，已在 `DatabaseTraceWriter` 按摘要字段长度截断，完整详情仍保留在 `detailJson`。

## 10. 架构整改专项验证：CONDITION / Compiler / EvalCase / RunNo

针对架构审核提出的运行契约与应用服务边界问题，本轮完成以下整改：

- CONDITION 条件对象统一为 `left/operator/right/valueType`，后端运行时、发布校验、前端提示示例与测试均不再使用 `path/value` 作为正式条件结构。
- CONDITION 新增 `NOT_CONTAINS`、`IN`、`NOT_IN` 运行语义与发布校验，`IN/NOT_IN` 要求 `right` 为数组。
- `DefaultWorkflowCompiler` 已承担 LangGraph4j `StateGraph` 构建和路由表构建职责，`CompiledWorkflow` 持有已编译执行图；`DefaultWorkflowRuntimeEngine` 只负责运行记录、上下文初始化、图调用和终态收口。
- LangGraph4j 图状态只保存可序列化执行标识，运行期回调由 `WorkflowGraphExecutionRegistry` 登记，避免把不可序列化 Lambda 放入 state。
- 新增 `EvalCaseApplicationService` 与 `DefaultEvalCaseApplicationService`，用例 create/update/get/list/confirm/archive/createFromNodeRun 已从 `EvalApplicationService` 拆出；REST 契约保持不变。
- 新增 `RunNoGenerator`，正式运行、AGENT_CALL 子运行、Eval AgentRun、EvalRun 均通过统一入口生成 `run_` / `eval_` 编号。
- `NodeExecutionRunner.execute`、`DefaultWorkflowRuntimeEngine.executeGraphNode`、`DefaultEvalApplicationService.runSuite` 已补充关键业务流程中文注释，说明事务边界、Trace/NodeRun 锚点、Schema/超时/失败收口语义。

增量定向验证：

```powershell
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q "-Dtest=NodeExecutorRuntimeSemanticsTests,DefaultWorkflowDraftValidationServiceTests,DefaultWorkflowCompilerTests,DefaultWorkflowRuntimeEngineTests,DefaultEvalApplicationServiceTests,RunNoGeneratorTests" test
```

- 退出码：0。
- 覆盖 CONDITION left/right、IN、NOT_IN、NOT_CONTAINS、发布校验拒绝旧 path/value、Compiler 产出 executionGraph、运行超时、EvalCase 正式化校验、RunNo 格式。

全量验证：

```powershell
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q test
npm test -- --run
npm run build
git diff --check
```

- 后端 Docker 全量 `mvn -q test` 退出码：0；Testcontainers 实际启动 PostgreSQL 16，并由 Flyway 从空库应用 `V1__create_core_tables.sql`。
- 前端 `npm test -- --run` 退出码：0，7 个测试文件、11 个用例通过。
- 前端 `npm run build` 退出码：0，Vite 仍提示主 chunk 超过 500 kB，为既有非阻塞提示。
- `git diff --check` 退出码：0；仅提示 Windows 行尾转换，不存在空白错误。
- `node_run.error_message` 是 NOT NULL，成功节点完成时写入 null 会失败，已在 `MyBatisNodeRunRepository` 将空错误消息归一为空字符串。

Poe 兼容性边界：

- Poe Chat Completions 可以用于真实模型调用、Trace、错误传播、提示词约束 JSON 输出、本地 JSON 解析与 Schema 校验链路验收。
- Poe 官方文档说明 Chat Completions 的 `response_format` 会被忽略，因此本轮不能证明“供应商强制结构化输出”能力。
- 若发布验收必须覆盖 provider-enforced structured output，仍需使用支持 `response_format` 的 OpenAI 兼容服务，或后续单独适配 Poe Responses API。

补充验证命令：

```powershell
docker run --rm --name myagent-v1-acceptance-build -v "D:\myproject\MyAgent\11_code\backend:/workspace" -w /workspace maven:3.9.9-eclipse-temurin-21 mvn -q -DskipTests package
docker run --rm --name myagent-v1-acceptance-test -v "D:\myproject\MyAgent\11_code\backend:/workspace" -v /var/run/docker.sock:/var/run/docker.sock -v myagent-maven-repo:/root/.m2 -w /workspace maven:3.9.9-eclipse-temurin-21 mvn -q test
npm test -- --run
npm run build
git diff --check
```

补充验证结果：

- Docker 后端 Jar 构建通过。
- Docker 内 Maven `mvn -q test` 通过；Testcontainers 可通过挂载 Docker socket 连接 Docker Desktop。
- 前端 `npm test -- --run` 通过：7 个测试文件、11 个测试用例通过。
- 前端 `npm run build` 通过；仍有 Vite chunk 体积提示，不影响验收。
- `git diff --check` 退出码为 0；仅提示 Windows 行尾转换，不存在空白错误。

2026-05-31 追加真实部署接口验收：

- `GET /actuator/health` 返回 `UP`，后端容器与 PostgreSQL 容器保持运行。
- `GET /v3/api-docs` 返回 `MyAgent API v1`，当前暴露 40 个 REST 路径，包含 `/api/runs`、`/api/runs/{runId}`、`/api/settings` 和 `/api/external-agents/{adapterId}/secrets`。
- `GET /api/runs` 可查询到真实 Poe 模型运行记录 `run_20260531205516_d30f894a`，状态为 `SUCCESS`。
- `GET /api/runs/{runId}` 返回 `nodeRunCount=3`、`traceEventCount=7`，Trace 类型包含 `MODEL_REQUEST`、`MODEL_RESPONSE`、`SCHEMA_VALIDATION`、`RUN_FINISHED`。
- Trace 对外字段已验证为 `detail`，未暴露旧字段 `detailJson`。
- `GET /api/settings` 仅返回 7 个白名单键，未返回 `myagent.openai.api-key` 或 `myagent.trace.persist-full-model-content`。
- `PUT /api/settings` 写入废弃键 `myagent.runtime.default-timeout-seconds` 返回统一响应 `success=false`，错误码为 `INVALID_ARGUMENT`。
- `POST /api/external-agents` 创建 `CUSTOM_HTTP` 外部 Agent 时，响应不回显敏感 header 明文，只返回 `secretConfigured=true` 元信息。
- `PUT /api/external-agents/{adapterId}` 普通更新不覆盖已有 secret，敏感 header 元信息仍显示 `secretConfigured=true`。
- 创建敏感 header 但不提供 secret 的 `CUSTOM_HTTP` 外部 Agent 后，`POST /api/external-agents/{adapterId}/test` 在真实外呼前失败，返回缺少密钥配置错误。
- 后端 Docker 内 `mvn -q test` 重新通过：76 个测试，0 失败，0 错误，2 个 PostgreSQL 集成环境跳过。
- `git check-ignore -v` 验证 `11_code/backend/target/` 和 `11_code/frontend/tsconfig.tsbuildinfo` 均命中 `.gitignore` 规则。

2026-05-31 架构审查三项收口修复：

- `createCaseFromNodeRun` 已补充来源一致性校验：来源 Agent、workflowVersion、nodeId 必须与 EvalSuite 绑定目标一致，NodeRun 必须为 `SUCCESS`，且 `outputJson` 不能为空。
- 正式 `POST /api/eval-suites/{suiteId}/runs` 已拒绝 `includeUnconfirmed=true`，错误消息为“V1 正式验收不允许包含未确认用例。”。
- EvalCase 仓储正式可执行用例查询固定过滤 `confirm_status in ('USER_CREATED', 'USER_CONFIRMED')`，不再通过 includeUnconfirmed 放开未确认用例。
- 新增 `WorkflowMappingValidationService`，发布校验阶段检查 `inputMapping`、`outputMapping` 的受控 JSONPath 语法，以及映射字段与 `inputSchemaRef`、`outputSchemaRef` 的字段关系。
- `inputMapping` 对象映射到 inputSchema 不存在字段、缺少 required 字段时会在发布校验阶段失败。
- `outputMapping` 读取 outputSchema 不存在字段、写入 `$.input` 时会在发布校验阶段失败。
- `DefaultMappingService` 的对象形态 `outputMapping` 已与设计文档收口为“写回目标路径 -> 节点输出来源路径”，例如 `{ "$.result.summary": "$.summary" }`。
- 新增 `DefaultEvalApplicationServiceTests` 覆盖正常生成 `AI_DRAFT_PENDING` 用例、Agent 不一致、workflowVersion 不一致、nodeId 不一致、NodeRun 非 SUCCESS、NodeRun 输出为空、正式 EvalRun 拒绝 includeUnconfirmed。
- 新增 `DefaultWorkflowDraftValidationServiceTests` 覆盖非法 inputMapping 字段、缺少 required 字段、非法 outputMapping 来源字段、写入 `$.input`、合法映射通过。
- `DefaultMappingServiceTests` 新增对象形态 outputMapping 写回测试。

本轮验证结果：

- 定向后端测试 `DefaultWorkflowDraftValidationServiceTests,DefaultEvalApplicationServiceTests,DefaultMappingServiceTests` 通过。
- Docker 内后端 `mvn -q test` 通过：89 个测试，0 失败，0 错误，2 个 PostgreSQL 集成环境跳过。
- 前端 `npm test -- --run` 通过：7 个测试文件、11 个测试用例通过。
- 前端 `npm run build` 通过；仍有 Vite chunk 体积提示，不影响本轮验收。

2026-06-01 EvalRun 事务边界专项修复：

- `DefaultEvalApplicationService.runSuite` 已移除完整方法级大事务，不再把 AgentRun、EvalRun、NodeRun 与节点执行包在同一个主线程事务里。
- 新增 `EvalRunLifecycleService`，将 `createEvalAgentRun`、`createEvalRun`、`insertEvalCaseResult`、`finishEvalRun`、`finishEvalAgentRun` 收口为独立 `REQUIRES_NEW` 小事务。
- Eval 正式运行在节点执行前已经提交 AgentRun 与 EvalRun；`NodeExecutionRunner` 在线程池内写 `MODEL_REQUEST`、`MODEL_RESPONSE` Trace 时可以看到已提交父记录。
- EvalCaseResult 按用例独立提交；后续 Trace 或用例执行异常不会回滚已发生的审计事实。
- 异常路径会显式更新 EvalRun 与配套 AgentRun 终态，按异常类型映射 `FAILED`、`TIMEOUT`、`CANCELED`。
- 删除前端无引用占位文件 `shared/forms/index.ts` 与 `shared/stores/index.ts`。

新增专项测试：

- 新增 `EvalRunTransactionBoundaryPostgresTests`，使用 Testcontainers 启动真实 `postgres:16-alpine`，通过 Flyway 从空库迁移 V1 表结构。
- 测试构造已确认 EvalSuite、USER_CONFIRMED EvalCase 和 LLM 目标节点，执行正式 `runSuite`。
- 验证最终 EvalRun 与 AgentRun 均为 `SUCCESS`，NodeRun 为 `SUCCESS`。
- 验证 Trace 包含 `MODEL_REQUEST`、`MODEL_RESPONSE`、`EVAL_CASE_RESULT`，且模型 Trace 同时带有 `run_id`、`node_run_id`、`eval_run_id`。

本轮验证命令：

```powershell
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q -Dtest=EvalRunTransactionBoundaryPostgresTests test
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q test
npm test -- --run
npm run build
```

本轮验证结果：

- `EvalRunTransactionBoundaryPostgresTests` 通过，Testcontainers 实际连接 Docker Desktop 并启动 PostgreSQL 16。
- Docker 内后端 `mvn -q test` 通过：90 个测试，0 失败，0 错误，0 跳过。
- 前端 `npm test -- --run` 通过：7 个测试文件、11 个测试用例通过。
- 前端 `npm run build` 通过；仍有 Vite chunk 体积提示，不影响本轮验收。
- `git diff --check` 退出码为 0；仅提示 Windows 行尾转换，不存在空白错误。

2026-06-01 EvalCase 正式断言与 outputSchema 失败输出保留收口：

- `DefaultEvalApplicationService` 已在用户创建正式用例、确认单个用例、确认套件、正式执行 EvalRun 前统一校验确定性断言。
- 正式 EvalCase 的 `assertionsJson` 必须是非空数组，每条断言必须是对象且具备非空 `type`，未知断言类型会被拒绝。
- `DefaultEvalAssertionEvaluator` 不再把空断言数组隐式判定为通过，空断言会返回 `INVALID_ASSERTIONS` 失败结果。
- `NodeOutputSchemaValidationException` 用于携带已经产生的节点输出；`NodeExecutionRunner` 在 outputSchema 校验失败时仍将该输出写入 `NodeRun.outputJson`，节点状态保持 `FAILED`。
- `LLM`、`REVIEW`、`SUMMARY`、`JAVA_METHOD`、`TOOL`、`EXTERNAL_AGENT`、`AGENT_CALL`、`END` 输出校验统一走 `validateOutputSchema`，避免 Trace 有输出但 NodeRun 丢失输出。
- 删除 `ExternalAgentApplicationService.refreshExternalAgentCatalog()` 空接口及实现，内部应用服务文档同步删除该无语义占位。
- `httpClient.ts` 注释已从“临时”改为“传输层”表述，避免误导后续重复封装。

新增或补充测试：

- `DefaultEvalApplicationServiceTests` 新增覆盖：创建正式用例拒绝空断言、确认用例拒绝空断言、确认套件拒绝空断言正式用例、正式 EvalRun 执行前拒绝未知断言类型且不创建运行记录。
- `DefaultEvalAssertionEvaluatorTests` 新增覆盖：空断言数组返回失败而不是通过。
- `NodeExecutorRuntimeSemanticsTests` 新增覆盖：模型已返回但 outputSchema 失败时，`NodeRunFinishRecord.outputJson` 保留原始输出，Trace 顺序包含 `MODEL_RESPONSE -> SCHEMA_VALIDATION -> NODE_ERROR`。

本轮验证命令：

```powershell
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q "-Dtest=DefaultEvalApplicationServiceTests,DefaultEvalAssertionEvaluatorTests,NodeExecutorRuntimeSemanticsTests" test
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q test
npm test -- --run
npm run build
```

本轮验证结果：

- 定向后端测试通过，覆盖本轮新增 EvalCase、断言执行器和 NodeExecutionRunner 输出保留语义。
- Docker 内后端 `mvn -q test` 通过：96 个测试，0 失败，0 错误，0 跳过；Testcontainers 实际连接 Docker Desktop 并启动 PostgreSQL 16。
- 前端 `npm test -- --run` 通过：7 个测试文件、11 个测试用例通过。
- 前端 `npm run build` 通过；仍有 Vite chunk 体积提示，不影响本轮验收。

2026-06-01 Eval SCHEMA_VALIDATION 断言真实语义收口：

- `SCHEMA_VALIDATION` 断言不再无条件通过，必须依赖节点执行期间真实产生的 `NODE_OUTPUT` Schema 校验结果。
- 新增 `SchemaValidationRecord`，运行期 Schema 校验结果会携带 `stage`，NodeRun 落库的 `schemaValidationResultJson.results[]` 现在包含 `stage`、`valid`、`schemaKey`、`version`、`errors`。
- `NodeExecutionResult` 新增 `schemaValidationResultJson`，Eval 执行断言时直接使用本次节点运行的 Schema 校验事实，不再由断言器自行假设。
- 正式 EvalCase 中如果包含 `SCHEMA_VALIDATION` 断言，创建、确认、确认套件、正式运行前都会校验目标节点必须配置 `outputSchema`；否则拒绝该用例进入正式验收链路。
- `DefaultEvalAssertionEvaluator` 在未找到成功的 `NODE_OUTPUT` 校验结果时返回失败：“未执行节点输出 Schema 校验。”。

新增或补充测试：

- `DefaultEvalAssertionEvaluatorTests` 覆盖 `SCHEMA_VALIDATION` 无输出 Schema 校验结果时失败、存在成功 `NODE_OUTPUT` 校验结果时通过。
- `DefaultEvalApplicationServiceTests` 覆盖目标节点未配置 `outputSchema` 时拒绝确认含 `SCHEMA_VALIDATION` 断言的 EvalCase。
- `NodeExecutorRuntimeSemanticsTests` 覆盖 outputSchema 失败时 `NodeExecutionResult.schemaValidationResultJson` 保留 `NODE_OUTPUT` 校验记录。

本轮验证命令：

```powershell
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q "-Dtest=DefaultEvalApplicationServiceTests,DefaultEvalAssertionEvaluatorTests,NodeExecutorRuntimeSemanticsTests" test
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q test
npm test -- --run
npm run build
```

本轮验证结果：

- 定向后端测试通过，覆盖 Eval 正式断言、SCHEMA_VALIDATION 真实校验结果依赖和 NodeExecutionRunner Schema 结果透传。
- Docker 内后端 `mvn -q test` 通过：99 个测试，0 失败，0 错误，0 跳过；Testcontainers 实际连接 Docker Desktop 并启动 PostgreSQL 16。
- 前端 `npm test -- --run` 通过：7 个测试文件、11 个测试用例通过。
- 前端 `npm run build` 通过；仍有 Vite chunk 体积提示，不影响本轮验收。

2026-06-01 WorkflowVersion 当前版本指针口径收口：

- `WorkflowVersionMapper.findCurrentDraft` 已改为从 `agent_definition.current_draft_workflow_version_id` 定位当前草稿版本，`workflow_version.status = 'DRAFT'` 仅作为一致性过滤。
- `WorkflowVersionMapper.findCurrentPublished` 已改为从 `agent_definition.current_published_workflow_version_id` 定位当前发布版本，`workflow_version.status = 'PUBLISHED'` 仅作为一致性过滤。
- 没有增加“指针为空时按 status 回退”的兼容逻辑，当前版本身份只保留 Agent 指针这一套事实来源。
- 运行链路原本已经按 Agent 发布指针读取，本轮没有改动运行链路。

新增专项测试：

- 新增 `WorkflowVersionCurrentPointerPostgresTests`，使用 Testcontainers 启动真实 PostgreSQL 并通过 Spring/MyBatis 仓储验证当前版本读取口径。
- 覆盖 `findCurrentDraft` 返回 Agent 草稿指针指向的 DRAFT 版本。
- 覆盖同 Agent 存在 HISTORY 版本时不会被误返回。
- 覆盖草稿指针为空时，即使存在 DRAFT 版本也返回空，不按 status 回退。
- 覆盖 `findCurrentPublished` 返回 Agent 发布指针指向的 PUBLISHED 版本。
- 覆盖发布指针为空时，即使存在 PUBLISHED 版本也返回空，不按 status 回退。

本轮验证命令：

```powershell
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q "-Dtest=WorkflowVersionCurrentPointerPostgresTests" test
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q test
```

本轮验证结果：

- `WorkflowVersionCurrentPointerPostgresTests` 通过，Testcontainers 实际连接 Docker Desktop 并启动 PostgreSQL 16。
- Docker 内后端 `mvn -q test` 通过：103 个测试，0 失败，0 错误，0 跳过。
- 本轮未改 DTO、OpenAPI 或前端代码，未重新生成前端类型。

## 10. 发布前仍需目标环境补验

以下项目不是架构口径问题，也不是本机开发阻塞项，但正式发布前必须在目标环境完成：

- 目标 PostgreSQL 数据库执行 Flyway 迁移并确认版本表状态。
- 如目标环境继续使用 Poe，需明确接受 Chat Completions 不强制 `response_format` 的边界；如必须验收供应商强制结构化输出，需换用支持 `response_format` 的兼容服务或适配 Poe Responses API。
- 在同一环境启动前端、后端和数据库，完整走通 `Schema -> Agent -> Workflow 保存/发布 -> Debug -> Runs -> NodeRun 生成 EvalCase -> EvalRun -> 历史对比`。

## 11. 开发侧结论

步骤 17 的开发侧质量闸门已通过：

- 前端单测通过。
- 前端生产构建通过。
- 后端 Maven 回归通过。
- 发布材料已输出。
- 遗留项均已归类为目标环境补验或发布后优化，不存在需要架构师继续澄清的实现口径问题。
## 2026-06-01 Eval 断言别名与 WorkflowContext 收口

本轮继续收口两处残留问题：

- Eval 断言正式契约只保留 `JSON_PATH_*` 与 `SCHEMA_VALIDATION`，不再接受 `FIELD_EQUALS`、`CONTAINS`、`NOT_CONTAINS`、`REGEX_MATCH`、`NUMERIC_RANGE`、`ENUM` 这类别名。
- `WorkflowContext.putNodeOutput()` 已移除 `root.withObject("/nodes")` 的 deprecated 入口，改为显式创建或复用 `nodes` 对象。

新增回归测试：

- `DefaultEvalAssertionEvaluatorTests.evaluateRejectsAliasAssertionType`
- `EvalCaseFormalValidationServiceTests.validateFormalEvalCaseRejectsAliasAssertionType`
- `WorkflowContextTests.putNodeOutputRecreatesNodesAfterReplaceRoot`

验证结果：

- 定向后端测试通过。
- Docker 内后端 `mvn test` 通过，`115` 个测试，`0` 失败，`0` 跳过。
- 前端 `npm test -- --run` 通过，`7` 个测试文件、`11` 个用例通过。
- 前端 `npm run build` 通过，仍有 Vite 主 chunk 超过 `500 kB` 的既有非阻塞提示。

## 2026-06-01 Eval expected 字段单一契约复验

本轮继续按架构审核意见确认 Eval 断言值字段收口：

- `DefaultEvalAssertionEvaluator` 只读取 `expected`，不再从 `value` 回退读取断言期望值。
- `EvalCaseFormalValidationService` 对正式 EvalCase 显式拒绝 `value` 字段，并要求 `JSON_PATH_EQUALS`、`JSON_PATH_CONTAINS`、`JSON_PATH_NOT_CONTAINS` 必须提供 `expected`。
- `ExternalAgentCommandJsonCodec` 已移除本轮涉及路径中的 `JsonNode.fields()` deprecated 用法。

本轮验证命令：

```powershell
docker run --rm -e DOCKER_HOST=unix:///var/run/docker.sock -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "D:\myproject\MyAgent:/workspace" -v "$env:USERPROFILE\.m2:/root/.m2" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/11_code/backend maven:3.9.9-eclipse-temurin-21 mvn -q test
npm test -- --run
npm run build
git diff --check
```

验证结果：

- Docker 内后端 `mvn -q test` 通过；Testcontainers 实际连接 Docker Desktop 并启动 PostgreSQL 16。
- 前端 `npm test -- --run` 通过：`7` 个测试文件、`11` 个用例通过。
- 前端 `npm run build` 通过；仍有 Vite chunk 超过 `500 kB` 的既有非阻塞提示。
- `git diff --check` 通过；仅有 Windows 行尾转换提示，不存在空白错误。
