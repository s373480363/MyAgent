# V1 二轮正式验收遗留问题复验记录 v1

## 1. 记录目的

本文档用于记录 2026-06-02 针对二轮正式验收遗留问题的测试侧复验结果。

本次复验只覆盖以下 3 项整改是否已收口：

- `RunsPage` 与最新 OpenAPI 契约的字段一致性
- 后端标准启动脚本在 PowerShell 下的可执行性
- `openapi:check` 质量闸门的正向通过与反向失败行为

本次复验结论不能替代完整 V1 全量终验结论。未在本轮重新执行的完整 V1 用例，仍以既有测试计划和后续正式终验为准。

## 2. 复验依据

- `8_change_list/agent-management-platform-v1/test_steps.md`
- `8_change_list/agent-management-platform-v1/status.md`
- `11_code/frontend/src/features/runs/pages/RunsPage.tsx`
- `11_code/scripts/start-backend-local.ps1`
- `11_code/frontend/scripts/download-openapi.ps1`
- `11_code/frontend/scripts/check-openapi-sync.ps1`
- `11_code/README.md`
- `13_release/02-V1部署说明-v1.md`

## 3. 复验环境

| 项目 | 结果 | 说明 |
|------|------|------|
| 复验日期 | 通过 | 2026-06-02 |
| 本机代码目录 | 通过 | `D:\myproject\MyAgent` |
| PostgreSQL | 通过 | 使用现有容器，连接 `127.0.0.1:15432/myagent` |
| 后端启动端口 | 通过 | 本轮实测按标准端口 `8080` 启动 |
| 前端启动端口 | 通过 | 本轮实测按标准端口 `5173` 启动 |
| 旧实例清理 | 通过 | 复验结束后确认 `8080/18080/5173/5174/5175/5176` 无监听进程 |

## 4. 代码层复核

### 4.1 RunsPage 契约字段

复核结果：

- `11_code/frontend/src/features/runs/pages/RunsPage.tsx` 已改为读取 `record.detail`
- 前端源码中未再检出 `detailJson` 残留引用

结论：

- 符合预期

### 4.2 后端标准启动脚本

复核结果：

- `11_code/scripts/start-backend-local.ps1` 已引入 `Set-StrictMode`
- 已设置 `$ErrorActionPreference = "Stop"`
- Maven 调用已改为 `& mvn "spring-boot:run" "-Dspring-boot.run.profiles=local"`

结论：

- 修复方向符合预期

### 4.3 OpenAPI 脚本链路

复核结果：

- `download-openapi.ps1` 已启用终止错误
- `Invoke-WebRequest` 已显式使用 `-ErrorAction Stop`
- 下载后已补充空文件检查与 JSON 解析检查
- `check-openapi-sync.ps1` 已启用终止错误并以脚本方式调用 `npm run openapi:refresh`

结论：

- 修复方向符合预期

## 5. 实际执行记录

### 5.1 前端自动化验证

执行目录：

```powershell
D:\myproject\MyAgent\11_code\frontend
```

执行命令：

```powershell
npm test -- --run
npm run build
```

执行结果：

- `npm test -- --run` 通过
- `7` 个测试文件通过
- `12` 个测试用例通过
- `npm run build` 通过

观察项：

- Vite 仍提示主 chunk 超过 `500 kB`
- jsdom 仍有 `getComputedStyle()` 相关非阻塞提示

判断：

- 不构成当前遗留问题复验阻塞

### 5.2 后端自动化验证

执行目录：

```powershell
D:\myproject\MyAgent\11_code\backend
```

执行命令：

```powershell
$env:JAVA_HOME='D:\myproject\MyAgent\9_dependency\tools\jdk21\jdk-21.0.11+10'
$env:Path='D:\myproject\MyAgent\9_dependency\tools\jdk21\jdk-21.0.11+10\bin;D:\myproject\MyAgent\9_dependency\tools\maven-3.9.11\apache-maven-3.9.11\bin;' + $env:Path
mvn -q test
```

执行结果：

- 后端测试通过
- Testcontainers 实际拉起 PostgreSQL 16
- Flyway 正常校验并应用迁移

判断：

- 不存在本轮整改引入的后端回归阻塞

### 5.3 标准后端启动脚本复验

执行命令：

```powershell
cd D:\myproject\MyAgent
powershell -NoProfile -ExecutionPolicy Bypass -File .\11_code\scripts\start-backend-local.ps1 `
  -DatasourceUrl 'jdbc:postgresql://127.0.0.1:15432/myagent' `
  -DatasourceUsername 'myagent' `
  -DatasourcePassword 'myagent'
```

执行结果：

- 后端成功启动
- 监听端口为 `8080`
- 日志确认激活 `local` profile
- 日志确认使用仓库内 `9_dependency\tools` 的 JDK 21 与 Maven 3.9.11

补充验证：

- `GET /api/ping` 返回成功
- `GET /api/agents?page=1&pageSize=20` 返回成功

判断：

- 启动脚本已从“文档可见但实际失败”修复为“标准入口可实际执行”

### 5.4 OpenAPI 正向闸门复验

执行命令：

```powershell
cd D:\myproject\MyAgent\11_code\frontend
npm run openapi:check
```

执行结果：

- 命令通过
- `openapi:refresh` 可正常下载快照并生成 `schema.ts`

判断：

- 正向质量闸门符合预期

### 5.5 OpenAPI 反向失败复验

执行命令：

```powershell
cd D:\myproject\MyAgent\11_code\frontend
$env:MYAGENT_OPENAPI_BASE_URL='http://127.0.0.1:59999'
npm run openapi:check
```

执行结果：

- 命令明确失败
- 退出码为非零
- 控制台出现 `Invoke-WebRequest : 无法连接到远程服务器`
- 失败前后 `openapi/myagent-openapi.json` 的 SHA-256 哈希一致

判断：

- 反向失败行为符合预期
- 本地快照未被失败下载污染

### 5.6 真实前后端联调复验

联调方式：

- 后端按 `8080` 启动
- 前端按 `5173` 启动
- 浏览器访问 `http://127.0.0.1:5173`

页面复验结果：

- `/agents` 页面正常渲染
- `/runs` 页面正常渲染
- `/runs/{runId}` 详情页正常渲染
- `Trace` 页签可正常加载事件列表
- 展开首条 Trace 事件后，页面成功展示详情 JSON
- 页面文本中未出现 `Failed to fetch`
- 页面文本中未出现 `detailJson`

联调判断：

- 前端代理链路工作正常
- Runs 详情页已实际消费 `detail` 字段
- 首轮遗留的 `Failed to fetch / detailJson` 相关阻塞已关闭

## 6. 复验结论

本次二轮正式验收遗留问题复验结论：

- 通过

结论依据：

1. `RunsPage` 的旧字段引用已清理，前端构建通过，Trace 展开详情可正常显示 `detail` 内容。
2. 后端标准启动脚本已能在 PowerShell 下按标准入口启动当前源码后端，不再出现 Maven 参数解析错误。
3. `openapi:check` 已同时满足“后端可达时通过”和“后端不可达时明确失败且不污染快照”。
4. 真实浏览器联调下，`/agents`、`/runs` 和运行详情页均可正常使用，未再出现旧契约或 CORS/代理阻塞表现。

## 7. 剩余观察项

本轮未发现新的 P1 阻塞问题，但保留以下非阻塞观察项：

- 前端生产构建仍有 Vite 大 chunk 提示，属于发布后优化项，不影响本轮复验结论。
- 浏览器侧存在 Ant Design 组件弃用告警，属于框架升级清理项，不影响本轮复验结论。

## 8. 与完整 V1 终验的关系

本记录只说明：

- 开发人员针对二轮遗留 3 项问题的修复内容，已经符合本轮整改预期。

本记录不说明：

- 完整 V1 全量能力已经在 2026-06-02 这轮被重新全量终验。

因此当前更准确的测试口径应为：

- “二轮正式验收遗留问题复验通过，整改内容符合预期。”
- “完整 V1 是否最终通过，仍需以完整测试计划的全量终验结论为准。”
