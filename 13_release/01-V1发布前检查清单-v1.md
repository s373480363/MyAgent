# Agent Studio V1 发布前检查清单

- [ ] `11_code/compose.yaml` 顶层包含 `name: agent-studio`
- [ ] 仅 `web` 暴露宿主机端口，映射为 `18080:80`
- [ ] `api` 与 `postgres` 不直接暴露宿主机端口
- [ ] PostgreSQL 契约固定为 `agent_studio` / `agent_studio`
- [ ] `AGENT_STUDIO_POSTGRES_PASSWORD` 已设置
- [ ] `AGENT_STUDIO_SECRET_KEY` 已设置，且为 Base64 编码的 32 字节随机值
- [ ] `AGENT_STUDIO_OPENAI_API_KEY` 已设置且不为空
- [ ] 缺失 `AGENT_STUDIO_SECRET_KEY` 时，`docker compose config` 会失败并提示 `AGENT_STUDIO_SECRET_KEY is required`
- [ ] 正式 OpenAI 变量只使用 `AGENT_STUDIO_OPENAI_*`
- [ ] 旧 OpenAI 变量在非测试启动路径会 fail-fast
- [ ] Maven 构件名为 `agent-studio-api`
- [ ] 前端 package name 为 `agent-studio-web`
- [ ] `frontend/openapi/agent-studio-openapi.json` 由 `/v3/api-docs` 下载生成
- [ ] `frontend/src/api/generated/schema.ts` 与 OpenAPI 快照同步
- [ ] 后端 `mvn -q test` 通过
- [ ] 前端 `npm test -- --run` 通过
- [ ] 前端 `npm run build` 通过
- [ ] 在 `D:\myproject\MyAgent\11_code` 下执行 `docker compose up -d --build` 可正常启动 `postgres`、`api`、`web`
- [ ] `/v3/api-docs` 返回的 `servers[0].url` 为 `http://127.0.0.1:18080`
- [ ] fresh install 时允许 `system_setting` 为空；若已有记录，则 `setting_key` 全部为 `agent.studio.*`
- [ ] [http://127.0.0.1:18080](http://127.0.0.1:18080) 可访问
