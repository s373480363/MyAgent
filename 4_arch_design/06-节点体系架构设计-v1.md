# 06-节点体系架构设计 v1

## 1. 节点体系原则

- 节点是工作流中最小可配置执行单元。
- 每个节点拥有独立 NodeRun。
- 节点输入输出通过 Schema 和映射表达。
- 节点能力通过 NodeExecutor 实现。
- v1 所有节点都进入统一 NodeDefinition 和 NodeExecutorRegistry。

## 2. NodeDefinition

```json
{
  "nodeId": "string",
  "type": "START | LLM | CONDITION | JAVA_METHOD | AGENT_CALL | EXTERNAL_AGENT | TOOL | REVIEW | SUMMARY | END",
  "name": "节点显示名称",
  "description": "节点职责说明",
  "inputSchemaRef": {
    "schemaKey": "string",
    "version": 1
  },
  "outputSchemaRef": {
    "schemaKey": "string",
    "version": 1
  },
  "inputMapping": {},
  "outputMapping": {},
  "timeoutSeconds": 120,
  "failurePolicy": "FAIL_FAST",
  "config": {},
  "ui": {
    "position": { "x": 0, "y": 0 }
  }
}
```

## 3. EdgeDefinition

```json
{
  "edgeId": "string",
  "sourceNodeId": "string",
  "targetNodeId": "string",
  "type": "NORMAL | CONDITION | DEFAULT | END",
  "condition": {},
  "description": "边说明"
}
```

`isDefault` 仅作为历史数据读取兼容字段存在。新建或编辑保存的 CONDITION 默认边必须使用 `type="DEFAULT"` 表达，显式条件边使用 `type="CONDITION"` 和 `condition` 表达；前端 UI 不得继续把 `isDefault` 作为新写入事实。

## 4. 节点职责

| 节点 | 职责 | 关键配置 |
|------|------|----------|
| START | 工作流入口 | 输入 Schema、示例输入 |
| LLM | 调用模型供应项 | 系统提示词、用户提示词模板、模型供应项、温度、输入 Schema、输出 Schema、允许工具 |
| CONDITION | 根据上下文选择分支 | 条件规则、默认分支（正常路由兜底）、错误处理、说明 |
| JAVA_METHOD | 调用平台注册 Java 方法 | 方法标识、入参映射、出参映射、超时 |
| AGENT_CALL | 调用平台内部已发布 Agent | 目标 Agent、输入映射、超时、失败策略 |
| EXTERNAL_AGENT | 调用平台外部 Agent | 适配器、提示词模板、工作目录、输出写回、超时 |
| TOOL | 调用平台注册工具 | 工具标识、参数映射、失败策略 |
| REVIEW | 审核输入或上游输出 | 审核标准、通过条件、失败提示、输出 Schema |
| SUMMARY | 总结上下文或上游结果 | 总结目标、长度限制、输出格式 |
| END | 工作流出口 | 最终输出映射 |

## 5. 发布校验规则

- 必须存在且只能存在一个 START 节点。
- 必须至少存在一个 END 节点。
- 除 START 外，每个可执行节点必须至少有一个入边。
- 除 END 外，每个可执行节点必须至少有一个出边。
- CONDITION 节点必须有默认分支。
- LLM、REVIEW、SUMMARY 必须有提示词模板或可继承的 Agent 默认提示词。
- JAVA_METHOD 必须引用已注册且启用的方法。
- AGENT_CALL 必须引用已发布目标 Agent，不能直接调用当前 Agent。
- EXTERNAL_AGENT 必须引用已注册且可用的外部 Agent 适配器。
- TOOL 必须引用已注册且启用的工具。
- 配置了 Schema 引用的节点必须确保引用存在，且按节点类型决定是否必填。
- 发布后引用的 Schema 版本锁定。
- 引用 Schema 的节点必须校验输入映射和输出映射字段存在，不能把不存在或不符合 Schema 的字段写入上下文。
- 节点级 timeoutSeconds 必须大于 0；未配置时使用节点类型默认超时。
- failurePolicy 必须属于节点类型支持的策略集合。

## 6. 条件规则

v1 CONDITION 节点使用声明式规则，不执行脚本。默认分支是正常路由兜底，不是错误兜底策略。建议支持：

- 字段等于。
- 字段不等于。
- 包含 / 不包含。
- 数值比较。
- 枚举匹配。
- 正常求值后未命中显式分支时，走默认分支。

复杂判断应通过 JAVA_METHOD 节点完成，再由 CONDITION 基于结构化结果分支。

CONDITION 的错误处理：

- 默认分支不是 `failurePolicy`，而是条件节点的正常路由兜底语义。
- `FAIL_FAST`：条件求值异常、字段缺失、输入结构不满足或类型不匹配时，节点失败。

v1 默认错误处理策略是 `FAIL_FAST`。默认分支必须配置，但默认分支只能用于正常求值后的路由兜底，不能掩盖条件求值错误。
