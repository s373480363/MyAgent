# 步骤 02 数据库迁移基线与核心表落地验证记录 v1

## 1. 验证范围

本记录对应 `8_change_list/agent-management-platform-v1/steps.md` 中的步骤 02：数据库迁移基线与核心表落地。

## 2. 已完成内容

- 新增 Flyway 迁移脚本：
  - `11_code/backend/src/main/resources/db/migration/V1__create_core_tables.sql`
- 迁移脚本已创建完整 V1 核心表：
  - `schema_definition`
  - `java_method_definition`
  - `tool_definition`
  - `external_agent_definition`
  - `agent_definition`
  - `workflow_version`
  - `system_setting`
  - `agent_run`
  - `node_run`
  - `trace_event`
  - `agent_message`
  - `eval_suite`
  - `eval_case`
  - `eval_run`
  - `eval_case_result`
- 已按用户确认的物理外键方案落地跨表关系。
- 已补齐工作流版本约束：
  - `uq_workflow_version_agent_version`
  - `uq_workflow_version_one_draft`
  - `uq_workflow_version_one_published`
- 已补齐运行态业务编号约束：
  - `agent_run.run_no`
  - `eval_run.run_no`
  - 未为 `node_run` 新增第二套对外编号。
- 已新增测试：
  - `DatabaseMigrationContractTests`
  - `PostgresMigrationTests`

## 3. 验证结果

- `mvn test` 已通过。
- `DatabaseMigrationContractTests` 通过，确认迁移脚本覆盖核心表、业务唯一约束和物理外键。
- `PostgresMigrationTests` 在当前环境被跳过，因为本机 Docker Desktop 未运行，Testcontainers 无法启动真实 PostgreSQL 容器。

## 4. 当前环境限制

当前开发机 Docker Desktop 服务未运行，且本机未发现可用 `psql` 命令，因此真实 PostgreSQL 集成测试需要在 Docker 或本地 PostgreSQL 可用后再次执行。

## 5. 结论

步骤 02 已完成，数据库迁移基线与核心表落地已满足当前开发实施方案要求。
