# 2026-06-06 代码层级验收记录 v1

## 1. 验收范围

本轮只做开发侧代码层级验收，目标是确认 `llm-node-eval-judge-rule-v1` 已按最新收口文档完成实现，且后端、前端与迁移在自动化层面没有阻断问题。

本轮不包含：

- 测试人员的真实业务验收
- 真实模型供应商联调
- 人工页面走查
- 生产或 Docker 正式部署复验

## 2. 验收环境

- 工作区：`D:\myproject\MyAgent`
- 验收日期：`2026-06-06`
- 后端 JDK：`9_dependency/tools/jdk21/jdk-21.0.11+10`
- 后端 Maven：`9_dependency/tools/maven-3.9.11/apache-maven-3.9.11`
- 前端 Node：`v24.12.0`
- 前端 npm：`11.6.2`

说明：

- 仓库内 `9_dependency/tools` 当前只提供了 JDK 和 Maven，没有单独提供 Node 发行版。
- 因此前端验收使用当前机器已安装的 Node/npm 完成。

## 3. 静态文档与契约检索

执行：

```powershell
rg -n "确定性优先但LLM 评分是可选辅助|scoreRule|referenceAnswer|failureReason|judgeRawOutput|judge_rule_json|judge_raw_output_json|includeUnconfirmed" D:\myproject\MyAgent\3_product_design D:\myproject\MyAgent\4_arch_design D:\myproject\MyAgent\6_schema_design D:\myproject\MyAgent\7_interface_design

rg -n --glob "!test_steps.md" "待用户确认|当前建议|需要用户最终确认|建议放在 EvalSuite|建议保留" D:\myproject\MyAgent\8_change_list\llm-node-eval-judge-rule-v1

rg -n "judgeRule|referenceSample|hardChecks|hardCheckResults|judgeResult|judgeRawText|judgePromptVersion|USER_CONFIRMED" D:\myproject\MyAgent\3_product_design D:\myproject\MyAgent\4_arch_design D:\myproject\MyAgent\6_schema_design D:\myproject\MyAgent\7_interface_design D:\myproject\MyAgent\8_change_list\llm-node-eval-judge-rule-v1
```

结果：

- 旧冲突契约关键字检索无命中。
- 变更包中未再保留“待用户确认 / 当前建议 / 建议保留”这类未冻结措辞。
- 新契约关键字已在产品、架构、Schema、接口和变更包文档中出现并互相对齐。

## 4. 后端自动化验收

执行：

```powershell
$env:JAVA_HOME = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\jdk21\jdk-21.0.11+10').Path
$mvnBin = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\maven-3.9.11\apache-maven-3.9.11\bin').Path
$env:PATH = "$env:JAVA_HOME\bin;$mvnBin;$env:PATH"
cd D:\myproject\MyAgent\11_code\backend
mvn clean test
```

结果：

- `BUILD SUCCESS`
- `Tests run: 119, Failures: 0, Errors: 0, Skipped: 0`
- 后端本轮使用 `clean test`，已排除旧 `target/test-classes` 残留造成的假通过风险。
- Flyway 全量迁移相关测试通过，说明当前 `V1__create_core_tables.sql` 与 `V4__model_provider_catalog_routing_v1.java` 能按最新契约工作。

本轮重点确认通过的契约点包括：

- EvalSuite 承载 judge 模型配置，EvalCase 不再承载 judge 模型字段。
- EvalCase 使用 `judgeRule`、`referenceSample`、`hardChecks`。
- `USER_CONFIRMED` 才能进入正式 EvalRun。
- hardChecks 失败时直接失败且跳过 judge，judge 相关结果字段为 `null`。
- `hardCheckResults` 固定为数组。
- EvalCaseResult 不再保留顶层 `score`。

## 5. 前端自动化验收

执行：

```powershell
cd D:\myproject\MyAgent\11_code\frontend
npm test -- --run
npm run build
```

结果：

- `vitest` 通过：`10` 个测试文件，`30` 个测试用例，`0` 失败。
- `tsc -b` 通过。
- `vite build` 通过。

本轮重点确认通过的契约点包括：

- EvalSuite 表单展示 judge 模型供应项和 judge 温度。
- EvalCase 表单展示 `judgeRule`、`referenceSample` 和 `hardChecks`。
- EvalCase 表单不再提供 judge 模型配置入口。
- 结果页按最新契约展示 `judgeResult`、`errorMessage`、`judgeRawText`、`judgePromptVersion` 和 `hardCheckResults`。
- hardChecks 失败跳过 judge 时，不展示顶层分数字段，分数只来自 `judgeResult.score`。

## 6. 非阻塞提示

- 前端单测期间出现 `jsdom` 对 `getComputedStyle(..., pseudo)` 的未实现提示，但不影响测试通过。
- 前端构建存在 Vite chunk 体积告警，不阻塞本变更当前验收。
- 后端测试存在 Mockito 自附加 agent 的未来 JDK 兼容性提示，当前不阻塞 `mvn clean test` 结果。

## 7. 结论

本轮代码层级验收通过，当前实现已满足 `llm-node-eval-judge-rule-v1` 最新正式文档约束，可以进入下一步架构复验和测试人员真实验收。

当前结论仅代表“开发实现已完成且自动化验收通过”，不替代后续人工业务验收和正式联调验收。
