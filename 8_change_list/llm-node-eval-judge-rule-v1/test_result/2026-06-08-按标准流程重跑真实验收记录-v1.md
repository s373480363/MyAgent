# 2026-06-08 按标准流程重跑真实验收记录 v1

## 1. 验收结论

本轮按 `AGENTS.md`、`0_specifications/test_specification.md`、`12_test_env/test_env.md`、`13_release/02-V1部署说明-v1.md` 和 `8_change_list/llm-node-eval-judge-rule-v1/test_steps.md`，从正式入口 `D:\myproject\MyAgent\11_code` 重新执行真实验收。

结论：**本轮未通过，且未进入真实业务验收阶段。**

阻塞原因不是本变更的字段契约回退，也不是后端/前端自动化失败，而是**正式 Docker 入口未能按文档成功启动**：

- `docker compose up -d --build` 在拉取 `nginx:1.27-alpine` 元数据时失败
- `docker pull nginx:1.27-alpine` 同样失败
- 宿主机直连 `https://registry-1.docker.io/v2/` 返回 `Connection was reset`
- `docker info` 显示当前 Docker daemon 配置了 `HTTP Proxy` / `HTTPS Proxy`

因此，本轮只能确认：

- 正式部署文件位置与必填环境变量口径正确
- 后端本机自动化测试通过，且 Testcontainers 实际连接 Docker Desktop
- 前端单测与生产构建通过

但**不能**确认：

- 正式 Docker 入口可启动
- `http://127.0.0.1:18080` 的真实服务可访问
- `/v3/api-docs`、`/api/settings`、`/actuator/health` 的正式入口联通
- 真实 EvalSuite / EvalCase / EvalRun 页面操作
- 真实 Poe judge LLM 调用链路

本轮未采用任何绕过手段，不使用本地手动启动的前后端替代正式 Docker 入口，也不复用旧镜像绕过当前启动失败。

## 2. 执行环境

- 验收日期：`2026-06-08`
- 工作目录：`D:\myproject\MyAgent`
- 正式部署目录：`D:\myproject\MyAgent\11_code`
- 正式部署文件：
  - `11_code/compose.yaml`
  - `11_code/.env`
- Docker Server：`29.1.5`
- Node：`v24.12.0`
- npm：`11.6.2`
- 后端工具链：
  - JDK：`9_dependency/tools/jdk21/jdk-21.0.11+10`
  - Maven：`9_dependency/tools/maven-3.9.11/apache-maven-3.9.11`

## 3. 正式入口执行记录

### 3.1 文档前置检查

执行：

```powershell
docker compose config --services
```

结果：

- 返回 `postgres`
- 返回 `api`
- 返回 `web`

对 `11_code/.env` 做非明文检查，以下变量均存在且非空：

- `AGENT_STUDIO_POSTGRES_PASSWORD`
- `AGENT_STUDIO_SECRET_KEY`
- `AGENT_STUDIO_OPENAI_API_KEY`

### 3.2 空库重启

执行：

```powershell
cd D:\myproject\MyAgent\11_code
docker compose down -v --remove-orphans
docker compose up -d --build
```

结果：

- `docker compose down -v --remove-orphans` 成功
- `docker compose up -d --build` 失败

失败点：

```text
failed to resolve source metadata for docker.io/library/nginx:1.27-alpine:
failed to do request:
Head "https://registry-1.docker.io/v2/library/nginx/manifests/1.27-alpine": EOF
```

### 3.3 环境问题复验

执行：

```powershell
docker pull nginx:1.27-alpine
curl.exe -I https://registry-1.docker.io/v2/
docker info
docker compose ps -a
curl.exe -I http://127.0.0.1:18080/actuator/health
```

结果：

- `docker pull nginx:1.27-alpine` 失败，错误与 Compose 一致
- 宿主机访问 Docker Hub registry 返回 `Recv failure: Connection was reset`
- `docker info` 显示当前 Docker daemon 存在代理配置：
  - `HTTP Proxy: http.docker.internal:3128`
  - `HTTPS Proxy: http.docker.internal:3128`
- `docker compose ps -a` 为空，说明本轮没有任何正式服务成功启动
- `curl http://127.0.0.1:18080/actuator/health` 失败，端口不可连接

结论：**当前机器在本轮执行时，不满足正式 Docker 入口所需的镜像拉取条件。**

### 3.4 浏览器侧补充说明

浏览器标签页仍能看到 `http://127.0.0.1:18080/` 的首页壳内容，但本轮命令行已同时确认：

- `18080` 当前没有监听进程
- 正式 Compose 服务未启动
- `/actuator/health` 无法连接

因此，这个浏览器页面不能作为“正式入口可用”的验收证据，只能视为缓存页面或历史状态残留。本轮**不据此判定通过**。

## 4. 自动化测试结果

## 4.1 后端

执行：

```powershell
$env:JAVA_HOME = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\jdk21\jdk-21.0.11+10').Path
$mvnBin = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\maven-3.9.11\apache-maven-3.9.11\bin').Path
$env:PATH = "$env:JAVA_HOME\bin;$mvnBin;$env:PATH"

cd D:\myproject\MyAgent\11_code\backend
mvn clean test
```

结果：

- `BUILD SUCCESS`
- `Tests run: 122`
- `Failures: 0`
- `Errors: 0`
- `Skipped: 0`

Testcontainers 证据：

- 已发现并连接 Docker Desktop
- 已启动 `testcontainers/ryuk:0.12.0`
- 已启动 `postgres:16-alpine`
- `PostgresMigrationTests`、`EvalRunTransactionBoundaryPostgresTests`、`WorkflowVersionCurrentPointerPostgresTests` 均真实执行

结论：**后端自动化通过。**

## 4.2 前端

执行：

```powershell
cd D:\myproject\MyAgent\11_code\frontend
npm test -- --run
npm run build
npm run openapi:check
```

结果：

- `npm test -- --run` 通过：`10` 个测试文件，`30` 个测试用例
- `npm run build` 通过
- `npm run openapi:check` 未通过

`openapi:check` 失败原因：

- `scripts/download-openapi.ps1` 需要访问正式入口 `/v3/api-docs`
- 由于本轮正式 Docker 入口未启动，`Invoke-WebRequest` 返回“无法连接到远程服务器”

结论：

- **前端单测与构建通过**
- **OpenAPI 同步检查因正式入口环境阻塞未能完成，不计为代码回归失败**

## 5. 静态文档验收补充

按 `test_steps.md` 执行静态检索时，有两点需要明确记录：

1. 检索旧冲突契约字段的命令返回空结果，这是符合预期的正向证据。
2. 检索“待用户确认 / 当前建议”之类措辞的命令会把 `test_result` 历史验收报告一并扫入，导致结果被历史记录污染。

这说明：

- 当前正式产品 / 架构 / schema / 接口文档没有发现本轮重点检查的旧冲突字段
- 但 `test_steps.md` 中这条 `rg` 命令的作用域过宽，容易把历史测试报告误当成当前设计文档问题

该问题属于**验收步骤文档噪音**，不属于本变更业务实现回退。

## 6. 最终判断

本轮按照“从文档重新启动、遇到环境问题必须暴露、不允许绕过正式入口”的要求执行后，给出的正式结论是：

- 本变更的代码自动化结果目前正常
- 但正式 Docker 入口在当前机器上不能按文档成功启动
- 因此本轮**不能**认定“真实验收通过”
- 后续若要继续真实验收，必须先解决当前 Docker Hub / Docker 代理访问问题，再从 `11_code` 重新执行一轮空库启动和正式入口验收
