# 2026-06-08 环境修复后按标准流程真实终验报告 v2

## 1. 终验结论

本轮按正式文档重新执行了完整终验入口：

- `AGENTS.md`
- `0_specifications/test_specification.md`
- `12_test_env/test_env.md`
- `13_release/02-V1部署说明-v1.md`
- `8_change_list/llm-node-eval-judge-rule-v1/test_steps.md`

结论：

- `llm-node-eval-judge-rule-v1` 主链能力已真实验收通过。
- 建议通过本次变更验收。
- 同时发现 1 项非阻塞基线偏差：`/api/settings` 实际返回 6 项，但发布文档和运行时 OpenAPI 描述仍写 7 项。该问题不属于本次 judgeRule / hardChecks / EvalRun 主链回归，但需要单独修订。

## 2. 验收范围与环境

- 验收日期：`2026-06-08`
- 工作目录：`D:\myproject\MyAgent`
- 正式部署入口：`D:\myproject\MyAgent\11_code`
- 正式访问地址：`http://127.0.0.1:18080`
- 正式部署文件：
  - `11_code/compose.yaml`
  - `11_code/.env`
- 后端工具链：
  - JDK：`9_dependency/tools/jdk21/jdk-21.0.11+10`
  - Maven：`9_dependency/tools/maven-3.9.11/apache-maven-3.9.11`
- 前端工具链：
  - Node：本机已安装
  - npm：本机已安装
- Docker：
  - Docker Desktop 可用
  - Testcontainers 实际连通 Docker Desktop

说明：

- 本轮没有绕过正式入口，没有改用本地手工启动前后端替代 Docker 入口。
- 本轮真实 LLM 调用使用容器内正式配置，不在报告中记录 API Key 明文。

## 3. 部署与基线检查

执行：

```powershell
cd D:\myproject\MyAgent\11_code
docker compose down -v --remove-orphans
docker compose up -d --build
curl.exe -sS http://127.0.0.1:18080/actuator/health
curl.exe -sS http://127.0.0.1:18080/v3/api-docs
docker compose exec -T postgres psql -U agent_studio -d agent_studio -c "select version from flyway_schema_history order by installed_rank;"
docker compose exec -T postgres psql -U agent_studio -d agent_studio -c "select setting_key from system_setting order by setting_key;"
```

结果：

- `postgres/api/web` 正式服务启动成功
- `GET /actuator/health` 返回 `{"status":"UP"}`
- `/v3/api-docs` 返回 `200`，`servers[0].url = http://127.0.0.1:18080`
- Flyway 版本为 `1, 2, 3, 4`
- fresh install 后 `system_setting` 为空表，符合正式文档约束
- 默认模型供应商存在：
  - `providerKey = openai-default`
  - `baseUrl = https://api.poe.com`
  - `apiKeyConfigured = true`
- 默认供应项存在：
  - `offeringKey = openai.gpt_5_4_mini`
- `POST /api/model-providers/1/test` 成功

结论：正式部署入口、空库初始化、正式模型连通性通过。

## 4. 自动化验收

### 4.1 后端

执行：

```powershell
$env:JAVA_HOME = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\jdk21\jdk-21.0.11+10').Path
$mvnBin = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\maven-3.9.11\apache-maven-3.9.11\bin').Path
$env:PATH = "$env:JAVA_HOME\bin;$mvnBin;$env:PATH"

cd D:\myproject\MyAgent\11_code\backend
mvn clean test
```

结果：

- `BUILD SUCCESS`
- `Tests run: 122`
- `Failures: 0`
- `Errors: 0`
- `Skipped: 0`

Testcontainers 证据：

- 实际连接 Docker Desktop
- 实际启动 `testcontainers/ryuk:0.12.0`
- 实际启动 `postgres:16-alpine`
- 以下集成测试真实执行，无跳过：
  - `MasterDataCatalogAvailabilityTests`
  - `PostgresMigrationTests`
  - `EvalRunTransactionBoundaryPostgresTests`
  - `WorkflowVersionCurrentPointerPostgresTests`

### 4.2 前端

执行：

```powershell
cd D:\myproject\MyAgent\11_code\frontend
npm test -- --run
npm run build
npm run openapi:check
```

结果：

- `10` 个测试文件通过
- `30` 个测试用例通过
- 前端生产构建通过
- OpenAPI generated schema 同步检查通过

非阻塞提示：

- Vitest 仍有 `jsdom getComputedStyle()` pseudo-element 提示
- Vite 仍有 chunk size warning

以上均不影响本次终验结论。

## 5. 静态文档复核

执行：

```powershell
rg -n "确定性优先|scoreRule|referenceAnswer|failureReason|judgeRawOutput|judge_rule_json|judge_raw_output_json|includeUnconfirmed" ^
  D:\myproject\MyAgent\3_product_design ^
  D:\myproject\MyAgent\4_arch_design ^
  D:\myproject\MyAgent\6_schema_design ^
  D:\myproject\MyAgent\7_interface_design

rg -n "judgeRule|referenceSample|hardChecks|hardCheckResults|judgeResult|judgeRawText|judgePromptVersion|USER_CONFIRMED" ^
  D:\myproject\MyAgent\3_product_design ^
  D:\myproject\MyAgent\4_arch_design ^
  D:\myproject\MyAgent\6_schema_design ^
  D:\myproject\MyAgent\7_interface_design ^
  D:\myproject\MyAgent\8_change_list\llm-node-eval-judge-rule-v1
```

结果：

- 正式 product / arch / schema / interface 文档中未再把旧冲突字段作为正式契约保留
- 新语义已统一出现：
  - `judgeRule`
  - `referenceSample`
  - `hardChecks`
  - `hardCheckResults`
  - `judgeResult`
  - `judgeRawText`
  - `judgePromptVersion`
  - `USER_CONFIRMED`
- `JSON_PATH_EQUALS` 只以“V1 不提供”的否定语义出现

结论：本次变更的正式契约收口通过。

## 6. 真实业务链路验收

### 6.1 新建正式测试数据

本轮通过正式 API 创建：

- 输入 Schema：
  - `schemaKey = accept-eval-judge-20260608174156.input`
  - `id = 6`
- 输出 Schema：
  - `schemaKey = accept-eval-judge-20260608174156.output`
  - `id = 7`
- Agent：
  - `agentId = 2`
  - `agentKey = accept-eval-judge-20260608174156`
- EvalSuite：
  - `suiteId = 1`

说明：

- 首次我构造测试工作流时，把 LLM 节点输出映射写成了 `$.output.summary`，导致 `START` 节点落到 `$.output.text` 的旧值没有被覆盖，`END` 节点输出校验失败。
- 该问题是本轮测试数据配置问题，不是项目业务回归缺陷。
- 将 LLM 节点 `outputMapping` 修正为 `$.output` 后，正式 published run 成功。

### 6.2 Published Run 成功验证

修正映射后执行：

- 发布工作流：`workflowVersionId = 6`
- 正式运行：`runId = run_20260608174626_c86d9f00`

结果：

- Run 状态：`SUCCESS`
- 最终输出：

```json
{
  "summary": "系统存在数据泄漏风险，应立即告警并冻结导出权限。"
}
```

结论：正式 Agent 发布运行链路可成功完成。

### 6.3 Eval judge 主链验证

执行的关键正式接口：

- `POST /api/eval-suites`
- `POST /api/eval-suites/{suiteId}/cases`
- `PUT /api/eval-suites/{suiteId}/cases/{caseId}/confirm`
- `POST /api/node-runs/{nodeRunId}/eval-cases`
- `PUT /api/eval-suites/{suiteId}/confirm`
- `POST /api/eval-suites/{suiteId}/runs`
- `GET /api/eval-runs/{evalRunId}`
- `GET /api/eval-runs/{evalRunId}/results`
- `GET /api/runs/{runId}`

#### A. 空 `judgeRule` 用例

- 创建成功
- 返回：
  - `caseId = 1`
  - `confirmStatus = USER_CREATED`
- 确认失败
- 返回：
  - `success = false`
  - `code = INVALID_ARGUMENT`
  - `message = 正式验收用例确认前必须填写 judgeRule：CASE-EMPTY-JUDGE`

结论：创建允许，确认拒绝，符合正式契约。

#### B. 从 `NodeRun` 生成 EvalCase

- 来源 `nodeRunId = 2`
- 创建成功
- 返回：
  - `caseId = 2`
  - `confirmStatus = AI_DRAFT_PENDING`
  - `sourceRunId = run_20260608174454_3199b377`
  - `sourceNodeRunId = 2`
  - `sourceWorkflowVersionId = 4`
  - `sourceNodeId = node-llm`
- `referenceSample` 真实复制自节点输出：

```json
{
  "summary": "系统存在数据泄漏风险，应立即告警并冻结导出权限。"
}
```

- 确认失败
- 返回：
  - `success = false`
  - `code = INVALID_ARGUMENT`
  - `message = 正式验收用例确认前必须填写 judgeRule：case_20260608174457_8cc7507f`

结论：NodeRun 生成草稿、复制 `referenceSample`、保持 `AI_DRAFT_PENDING`、空 `judgeRule` 不可确认，符合正式契约。

#### C. 正式通过用例

- `caseId = 3`
- `caseNo = CASE-PASS`
- 确认后状态：`USER_CONFIRMED`

结果：

- hardChecks 全部通过
- judge LLM 被真实调用
- `passed = true`
- `judgeResult` 有值
- `judgeRawText` 有值
- `judgeModelOfferingKey = openai.gpt_5_4_mini`
- `judgePromptVersion = JUDGE_RULE_V1`
- 顶层无 `score` 字段，分数只存在于 `judgeResult.score`

实际结果片段：

```json
{
  "passed": true,
  "judgeResult": {
    "score": 1,
    "passed": true
  },
  "judgePromptVersion": "JUDGE_RULE_V1"
}
```

#### D. hardChecks 失败用例

- `caseId = 4`
- `caseNo = CASE-HARDCHECK-FAIL`
- 确认后状态：`USER_CONFIRMED`

结果：

- hardChecks 失败
- judge LLM 未调用
- `passed = false`
- `judgeResult = null`
- `judgeRawText = null`
- `judgeModelOfferingKey = null`
- `judgePromptVersion = null`
- `errorMessage = hardChecks 未通过，已跳过 judge LLM。`

#### E. EvalRun

- `suiteId = 1`
- `evalRunId = eval_20260608174458_d7dea50c`
- `runId = run_20260608174458_e2ae0e9f`

结果：

- 只执行了 `USER_CONFIRMED` 的 2 条用例
- `USER_CREATED` 和 `AI_DRAFT_PENDING` 未进入正式 EvalRun
- 汇总：
  - `status = FAILED`
  - `passRate = 50.00`
  - `totalCaseCount = 2`
  - `passedCaseCount = 1`
  - `failedCaseCount = 1`
- 失败摘要仅来自 hardChecks 失败用例：

```json
[
  {
    "caseNo": "CASE-HARDCHECK-FAIL",
    "reason": "hardChecks 未通过，已跳过 judge LLM。"
  }
]
```

- 运行 Trace 事件数：`10`

结论：本次变更最核心的真实业务链路已全部通过：

- `EvalSuite` 管理 judge 模型
- `EvalCase` 管理 `judgeRule/referenceSample/hardChecks`
- 只有 `USER_CONFIRMED` 进入正式运行
- hardChecks 失败跳过 judge
- pass rate 按 `EvalCaseResult.passed` 统计
- 结果字段契约与正式设计一致

## 7. 数据库核验

执行：

```powershell
docker compose exec -T postgres psql -U agent_studio -d agent_studio -c "select column_name from information_schema.columns where table_schema='public' and table_name='eval_case' order by ordinal_position;"
docker compose exec -T postgres psql -U agent_studio -d agent_studio -c "select column_name, column_default from information_schema.columns where table_schema='public' and table_name='eval_case_result' order by ordinal_position;"
docker compose exec -T postgres psql -U agent_studio -d agent_studio -c "select count(*) from information_schema.columns where table_schema='public' and table_name='eval_case_result' and column_name='score';"
docker compose exec -T postgres psql -U agent_studio -d agent_studio -c "select count(*) from information_schema.columns where table_schema='public' and table_name='eval_case' and column_name in ('judge_rule_json','score_rule_json','reference_answer_json');"
docker compose exec -T postgres psql -U agent_studio -d agent_studio -c "select eval_case_id, jsonb_typeof(hard_check_result_json) as hard_check_result_type from eval_case_result order by eval_case_id;"
```

结果：

- `eval_case` 包含：
  - `reference_sample_json`
  - `judge_rule_text`
  - `hard_checks_json`
- `eval_case` 不包含：
  - `judge_rule_json`
  - `score_rule_json`
  - `reference_answer_json`
- `eval_case_result.hard_check_result_json` 默认值为 `[]`
- `eval_case_result` 无顶层 `score` 列
- 已落库结果中，`hard_check_result_json` 的 JSON 类型均为 `array`
- hardChecks 失败用例的 judge 相关列均为 `null`

结论：DDL 与正式契约一致。

## 8. 正式前端页面验收

实机访问：

- `/`
- `/evals`
- 套件详情弹窗
- 运行详情弹窗
- 结果明细展开区

结果：

- 首页可打开，标题为 `Agent Studio 管理台`
- `/evals` 可打开
- Suite 列表真实显示：
  - Judge 模型
  - 温度
  - 阈值
  - 状态
- 用例页真实显示：
  - `USER_CREATED`
  - `AI_DRAFT_PENDING`
  - `USER_CONFIRMED`
  - 输入
  - `referenceSample`
  - `judgeRule`
  - `hardChecks`
- 运行结果页真实显示：
  - `judgeResult`
  - `judgeRawText`
  - `judgePromptVersion`
  - `judgeModelOfferingKey`
  - `hardCheckResults`
  - `errorMessage`
- hardChecks 失败用例的 Judge 分数显示为 `-`
- 浏览器控制台 `error` / `warn` 均为空

结论：正式前端页面与本次变更语义一致。

## 9. 发现的问题

### P2 非阻塞基线偏差：`/api/settings` 文档与实现不一致

现象：

- `GET /api/settings` 实际只返回 `6` 项
- 运行时 OpenAPI 仍声明“只返回 V1 白名单中的 7 个系统设置项”
- 发布文档也写“`/api/settings` 返回 7 个白名单项”

证据：

- 运行时响应：实际 6 项
- 代码：
  - `11_code/backend/src/main/java/com/myagent/settings/web/SettingController.java:49`
  - `11_code/backend/src/main/java/com/myagent/settings/domain/SettingsCatalog.java`
- 文档：
  - `13_release/02-V1部署说明-v1.md:86`

判断：

- 该问题不影响 `llm-node-eval-judge-rule-v1` 的 judgeRule / hardChecks / EvalRun 主链验收
- 但它确实构成“基线文档 / OpenAPI 描述 / 运行实现”不一致

建议：

- 单独修订 `/api/settings` 的描述，统一为真实值 6
- 或补齐第 7 项白名单配置，并同步后端实现、OpenAPI、发布文档

## 10. 最终建议

- 本次变更 `llm-node-eval-judge-rule-v1` 建议通过真实验收。
- `/api/settings` 的“6/7 项”偏差建议单独登记并修复，不阻塞本次变更关闭。
