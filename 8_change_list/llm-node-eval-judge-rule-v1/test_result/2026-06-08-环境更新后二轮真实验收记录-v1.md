# 2026-06-08 环境更新后二轮真实验收记录 v1

## 1. 验收范围

本轮按更新后的 `12_test_env/test_env.md` 重新执行验收，重点确认：

- 后端自动化测试是否使用本机 JDK/Maven 工具链完整执行。
- Testcontainers 是否能访问 Docker Desktop 并真实启动 PostgreSQL。
- 前端测试、构建和 OpenAPI generated contract 是否通过。
- 正式 Docker 入口是否可用。
- 真实 Poe 模型连接和 EvalRun 链路是否可执行。
- 上一轮文档类待整改项是否已关闭。

本轮没有清空 Docker volume，因此不能作为“空库首次初始化”的完整复验结论；当前正式入口使用的是既有 Compose 数据库。

## 2. 环境

- 验收日期：`2026-06-08`
- 工作区：`D:\myproject\MyAgent`
- Docker Server：`29.1.5`
- Docker Compose：`v5.0.1`
- 后端 JDK：`9_dependency/tools/jdk21/jdk-21.0.11+10`
- 后端 Maven：`9_dependency/tools/maven-3.9.11/apache-maven-3.9.11`
- 正式入口：`http://127.0.0.1:18080`
- 模型供应项：`openai.gpt_5_4_mini`

敏感信息说明：

- Poe API Key 来自 `12_test_env/test_env.md`。
- 本报告不记录 API Key 明文。

## 3. 后端自动化测试

执行：

```powershell
$env:JAVA_HOME = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\jdk21\jdk-21.0.11+10').Path
$mvnBin = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\maven-3.9.11\apache-maven-3.9.11\bin').Path
$env:PATH = "$env:JAVA_HOME\bin;$mvnBin;$env:PATH"

cd D:\myproject\MyAgent\11_code\backend
mvn clean test
```

结果：

- `BUILD SUCCESS`
- `Tests run: 119`
- `Failures: 0`
- `Errors: 0`
- `Skipped: 0`

Testcontainers 执行情况：

- 已连接 Docker Desktop。
- 已实际启动 `postgres:16-alpine`。
- `PostgresMigrationTests`、`EvalRunTransactionBoundaryPostgresTests`、`WorkflowVersionCurrentPointerPostgresTests` 均实际执行，未跳过。

结论：

- 上一轮 P3 环境问题已消除。
- 本轮后端自动化测试可以认定为完整通过。

## 4. 前端自动化测试与构建

执行：

```powershell
cd D:\myproject\MyAgent\11_code\frontend
npm test -- --run
npm run build
npm run openapi:check
```

结果：

- `npm test -- --run`：`10` 个测试文件通过，`30` 个测试通过。
- `npm run build`：通过。
- `npm run openapi:check`：通过。

非阻塞提示：

- Vitest 仍有 `jsdom` 关于 pseudo-elements 的 `getComputedStyle` 未实现提示，不影响断言结果。
- Vite 仍有 chunk 体积告警，不阻塞本轮验收。

结论：

- 上一轮 P1-02 OpenAPI generated schema 未同步问题在当前工作区已不复现。

## 5. 正式 Docker 入口

执行：

```powershell
docker compose ps
Invoke-RestMethod -Uri 'http://127.0.0.1:18080/actuator/health'
Invoke-RestMethod -Uri 'http://127.0.0.1:18080/v3/api-docs'
Invoke-WebRequest -Uri 'http://127.0.0.1:18080'
```

结果：

- `postgres`：运行中，健康状态 `healthy`。
- `api`：运行中。
- `web`：运行中，`18080` 已暴露。
- `/actuator/health`：`UP`。
- `/v3/api-docs`：`200`，包含 `50` 个 paths、`144` 个 schemas。
- Web 首页：`200`。

说明：

- 当前 Compose 服务使用既有数据库 volume。
- 本轮没有清空 volume 后重建，因此未覆盖“空库首次初始化”场景。

## 6. 模型供应商与真实模型调用

检查模型供应商：

- 默认供应商 `openai-default` 当前 `baseUrl=https://api.poe.com`。
- API Key 已配置。
- 供应项 `openai.gpt_5_4_mini` 状态为 `ENABLED` 且可选。

执行连接测试：

```powershell
POST http://127.0.0.1:18080/api/model-providers/1/test
```

请求体：

```json
{
  "offeringKey": "openai.gpt_5_4_mini",
  "prompt": "ping"
}
```

结果：

- 返回 `success=true`。
- `providerKey=openai-default`。
- `offeringKey=openai.gpt_5_4_mini`。
- `upstreamModelName=gpt-5.4-mini`。
- `durationMs=1243`。

结论：

- 当前正式入口下 Poe 模型连接测试通过。
- 上一轮 `baseUrl=https://api.poe.com/v1` 导致 404 的问题在当前既有数据库配置下不复现。

## 7. 真实 EvalRun 复验

复用当前正式环境中已确认的 EvalSuite：

- `suiteId=1`
- `workflowVersionId=4`
- `nodeId=llm_eval`
- `judgeModelOfferingKey=openai.gpt_5_4_mini`
- `passThreshold=50`

执行：

```powershell
POST http://127.0.0.1:18080/api/eval-suites/1/runs
```

结果：

- `evalRunId=eval_20260608023844_c7f686b2`
- `runId=run_20260608023844_67a610b9`
- `status=SUCCESS`
- `totalCaseCount=2`
- `passedCaseCount=1`
- `failedCaseCount=1`
- `passRate=50.00`

结果明细：

- `case_judge_pass`：`passed=true`，hardChecks 全部通过，保存 `judgeResult.score=92`、`judgeRawText`、`judgeModelOfferingKey=openai.gpt_5_4_mini`、`judgePromptVersion=JUDGE_RULE_V1`。
- `case_hardcheck_fail_skip_judge`：`passed=false`，hardChecks 失败，`judgeResult`、`judgeRawText`、`judgeModelOfferingKey`、`judgePromptVersion` 均为 `null`，错误摘要为 hardChecks 未通过并跳过 judge LLM。

结论：

- 真实 EvalRun 链路通过。
- `USER_CONFIRMED` 用例过滤、hardChecks、judge LLM、结果字段和通过率统计均符合本变更核心契约。

## 8. 文档与用户手册复查

执行静态检索：

```powershell
rg -n "参考答案|断言结果|评分结果|失败断言|referenceAnswer|assertions|scoreRule|judgeRawOutput|judge_rule_json|includeUnconfirmed" D:\myproject\MyAgent\3_product_design D:\myproject\MyAgent\4_arch_design D:\myproject\MyAgent\5_ui_design D:\myproject\MyAgent\6_schema_design D:\myproject\MyAgent\7_interface_design D:\myproject\MyAgent\14_user_manual
```

仍命中：

- `5_ui_design/Agent管理平台-前端组件清单与交互规范-v1.html:862`：仍写“参考答案、断言规则”。
- `5_ui_design/Agent管理平台-前端组件清单与交互规范-v1.html:1138`：仍写“参考答案、断言”。
- `3_product_design/Agent管理平台-产品设计-09-信息架构与验收标准-v1.md:62`：仍写“断言结果”。
- `4_arch_design/08-Trace追踪与调试架构设计-v1.md:74`：仍写“断言结果、评分结果、失败原因”。
- `4_arch_design/12-公共接口架构设计-v1.md:93`：仍写“失败断言”。

说明：

- `6_schema_design/05-运行追踪与验收表DDL-v1.md:324` 命中的是“不得再读取或更新旧字段”的否定说明，不计为问题。

用户手册：

- `14_user_manual/README.md` 长度仍为 `0`。

结论：

- 上一轮 P2-01 正式文档旧术语仍未关闭。
- 上一轮 P2-02 用户手册为空仍未关闭。

## 9. 本轮结论

本轮验收结果：

- 后端自动化环境问题已关闭：Testcontainers 实际执行，`Skipped=0`。
- 前端测试、构建和 OpenAPI 同步通过。
- 正式 Docker 入口可用。
- Poe 模型供应商连接测试通过。
- 真实 EvalRun 链路通过。

仍需整改：

- P2-01：正式文档仍残留旧“参考答案 / 断言 / 评分”语义。
- P2-02：`14_user_manual/README.md` 仍为空。

未完整覆盖：

- 本轮没有清空 Docker volume 后重建，因此不能证明 Docker 空库首次初始化已关闭 `baseUrl=https://api.poe.com/v1` 的历史风险。
