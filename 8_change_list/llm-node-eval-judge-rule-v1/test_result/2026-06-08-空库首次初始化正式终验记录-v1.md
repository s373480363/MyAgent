# 2026-06-08 空库首次初始化正式终验记录 v1

## 1. 终验结论

本轮终验按正式 Docker 入口、空库首次初始化、本机工具链自动化和真实业务链路四条要求执行。

结论：**未通过终验，需复验。**

阻塞原因不是字段契约回退，也不是自动化测试失败，而是当前 `12_test_env/test_env.md` 提供的 Poe 凭据在正式容器内真实调用时返回 `401 invalid_api_key`，导致：

- `POST /api/model-providers/1/test` 失败
- 修正映射后的真实 DebugRun 失败
- 真实 EvalRun 无法进入 `hardChecks -> judge` 的正式业务分支

因此，本轮可以确认：

- 空库首次初始化链路已通过
- 默认供应商 Base URL 归一化已通过
- 后端、前端、OpenAPI、正式文档和用户手册已通过
- 正式前端入口可访问

但**不能**确认：

- hardChecks 通过后 judge LLM 被真实调用
- hardChecks 失败后跳过 judge 的真实运行分支
- 从成功 LLM NodeRun 生成 `AI_DRAFT_PENDING` 草稿用例

## 2. 验收范围与环境

- 验收日期：`2026-06-08`
- 工作目录：`D:\myproject\MyAgent`
- 正式入口：`http://127.0.0.1:18080`
- 正式部署文件：`compose.yaml`
- 后端工具链：
  - JDK：`9_dependency/tools/jdk21/jdk-21.0.11+10`
  - Maven：`9_dependency/tools/maven-3.9.11/apache-maven-3.9.11`
- 前端工具链：
  - Node：`v24.12.0`
  - npm：`11.6.2`
- Docker：
  - Server：`29.1.5`
  - Compose：桌面版内置 Compose
- 模型配置：
  - Base URL：`https://api.poe.com`
  - Model：`gpt-5.4-mini`
  - API Key：来自 `12_test_env/test_env.md`，本报告不记录明文

说明：

- 本轮先执行 `docker compose down -v --remove-orphans`，明确删除 `agent-studio_postgres-data` volume。
- `api` 镜像基于当前代码重新构建。
- `web` 容器本轮复用本机已有 `agent-studio-web:latest` 镜像。原因是 Docker Hub 临时返回 EOF，无法重新拉取 `nginx:1.27-alpine` 元数据；但当前工作区不存在前端代码变更，且 `npm test -- --run`、`npm run build`、`npm run openapi:check` 均已通过，因此不影响本轮对正式入口与前端契约的结论。

## 3. 空库首次初始化

执行：

```powershell
docker compose down -v --remove-orphans
docker build -t agent-studio-api D:\myproject\MyAgent\11_code\backend
docker compose up -d --no-build
```

结果：

- 正式 `postgres/api/web` 服务成功启动
- `GET /actuator/health` 返回 `UP`
- `GET /v3/api-docs` 返回 `200`
- API 启动日志明确出现：
  - `Current version of schema "public": << Empty Schema >>`
  - `Successfully applied 4 migrations to schema "public", now at version v4`

首次初始化后的正式供应商检查：

- `GET /api/model-providers`
- 返回 `providerKey=openai-default`
- 返回 `baseUrl=https://api.poe.com`
- 未再出现旧问题中的 `https://api.poe.com/v1`

结论：**空库首次初始化通过。**

## 4. 自动化验收

### 4.1 后端

执行：

```powershell
$env:JAVA_HOME = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\jdk21\jdk-21.0.11+10').Path
$mvnBin = (Resolve-Path 'D:\myproject\MyAgent\9_dependency\tools\maven-3.9.11\apache-maven-3.9.11\bin').Path
$env:PATH = "$env:JAVA_HOME\bin;$mvnBin;$env:PATH"
mvn clean test
```

结果：

- `BUILD SUCCESS`
- `Tests run: 122`
- `Failures: 0`
- `Errors: 0`
- `Skipped: 0`

Testcontainers 证据：

- 已连接 Docker Desktop
- 已实际启动 `testcontainers/ryuk:0.12.0`
- 已实际启动 `postgres:16-alpine`
- `PostgresMigrationTests`
- `EvalRunTransactionBoundaryPostgresTests`
- `WorkflowVersionCurrentPointerPostgresTests`

均已真实执行，无跳过。

说明：

- 相比上一轮 `119`，本轮为 `122`，原因是当前工作区新增了 `ModelCatalogBootstrapDefaultsTests` 等修复验证。

结论：**后端自动化通过。**

### 4.2 前端

执行：

```powershell
npm test -- --run
npm run build
npm run openapi:check
```

结果：

- `10` 个测试文件通过
- `30` 个测试用例通过
- 前端生产构建通过
- OpenAPI generated schema 同步检查通过

非阻塞提示：

- Vitest 仍有 `jsdom getComputedStyle()` 的 pseudo-element 提示
- Vite 仍有大 chunk 警告

均不影响本次验收结论。

结论：**前端自动化通过。**

## 5. 文档与用户手册复查

执行静态检索：

```powershell
rg -n "参考答案|断言结果|评分结果|失败断言|referenceAnswer|assertions|scoreRule|judgeRawOutput|judge_rule_json|includeUnconfirmed" \
  D:\myproject\MyAgent\3_product_design \
  D:\myproject\MyAgent\4_arch_design \
  D:\myproject\MyAgent\5_ui_design \
  D:\myproject\MyAgent\6_schema_design \
  D:\myproject\MyAgent\7_interface_design \
  D:\myproject\MyAgent\14_user_manual
```

结果：

- 只命中 `6_schema_design/05-运行追踪与验收表DDL-v1.md:324`
- 该处语义为“Java migration 中不得再读取或更新旧字段”
- 属于否定说明，不计为问题

用户手册：

- `14_user_manual/README.md` 长度为 `3590`
- 已非空
- 内容已覆盖 EvalSuite、EvalCase、确认、NodeRun 草稿、正式运行、结果解读和 hardChecks/judgeRule/referenceSample 边界

结论：**正式文档与用户手册通过。**

## 6. 正式前端入口

浏览器实际访问：

- `/`
- `/evals`

结果：

- 首页可打开，标题为“Agent Studio 管理台”
- 侧边栏包含“节点验收”
- `/evals` 页面可打开
- 页面表格已显示正式字段列：
  - 套件
  - Agent
  - 工作流版本
  - 节点
  - Judge 模型
  - 温度
  - 阈值
  - 状态

结论：**正式前端入口通过。**

## 7. 真实业务链路执行

### 7.1 新建正式测试数据

本轮在空库中通过正式 API 创建：

- Agent：`agentId=2`
- AgentKey：`accept-eval-judge-20260608151150`
- 输入 Schema：`accept.eval.input.20260608151150`
- 输出 Schema：`accept.eval.output.20260608151150`
- 首次发布工作流：`workflowVersionId=6`

说明：

- 我在本轮新建了最小测试工作流。
- 首次构造时，LLM 节点缺少显式 `inputMapping/outputMapping`，导致 DebugRun 先死在节点输入 Schema 校验。
- 该问题属于**本轮新增测试数据配置不完整**，不是本变更开发任务的回归缺陷。
- 修正后重新保存草稿、校验并发布到：`workflowVersionId=9`
- 修正后的 LLM 节点正式包含：
  - `inputMapping = { "text": "$.input.text" }`
  - `outputMapping = "$.output"`

### 7.2 正式业务契约已验证的部分

已通过正式 API 验证：

- 创建 EvalSuite 成功，judge 模型配置只位于 EvalSuite
- 创建空 `judgeRule` 用例成功
- 空 `judgeRule` 用例确认失败，返回：
  - `code=INVALID_ARGUMENT`
  - `message=正式验收用例确认前必须填写 judgeRule`
- 可创建并确认 `USER_CONFIRMED` 正式用例
- 正式套件可确认为 `CONFIRMED`

### 7.3 真实 LLM 调用阻塞

修正映射后的正式调试运行：

- `runId=run_20260608151855_145a28f6`
- `START` 节点成功
- `LLM` 节点输入 Schema 校验成功
- 随后 LLM 节点在真实模型调用阶段失败

正式供应商连接测试：

```http
POST /api/model-providers/1/test
```

结果：

- 返回 `success=false`
- 返回中文错误：`模型调用失败，请检查模型供应商配置、模型名称或网络连接。`

API 容器日志中的真实上游错误：

```text
401 - {"error": {"message": "Incorrect API key provided. You can find your API key at https://poe.com/api/keys.", "type": "authentication_error", "code": "invalid_api_key"}}
```

这说明：

- Base URL 已正确
- 模型供应项路由已正确
- 问题不在 `judgeRule` 契约
- 问题不在 `hardChecks` 契约
- 问题也不在空库初始化逻辑
- 真正阻塞点是 **当前 `test_env.md` 中的 Poe API Key 已失效或不可用**

### 7.4 因上游凭据失效而无法完成的正式验证项

由于真实 LLM 调用被 `401 invalid_api_key` 阻塞，本轮**无法完成**以下正式终验点：

- 从成功的 LLM NodeRun 生成 `AI_DRAFT_PENDING` EvalCase
- hardChecks 通过后真实调用 judge LLM
- hardChecks 失败后真实跳过 judge LLM
- `judgeResult.score`、`judgeRawText`、`judgePromptVersion` 的真实落库
- `hardCheckResults` 在真实执行分支中的正式结果数组验证

### 7.5 当前 EvalRun 结果

在修正映射后的 `workflowVersionId=9` 上，重新创建正式套件并执行：

- `suiteId=2`
- `evalRunId=eval_20260608152802_2b8d513a`
- `runId=run_20260608152802_befd30bc`

结果：

- `status=FAILED`
- `passRate=0.00`
- `totalCaseCount=2`
- `passedCaseCount=0`
- `failedCaseCount=2`

失败原因统一为：

- `模型调用失败，请检查模型供应商配置、模型名称或网络连接。`

说明：

- 本结果不能用来否定 `hardChecks` 或 `judge` 主链设计
- 因为运行尚未进入有效 LLM 输出与 judge 分支，当前失败由外部凭据阻塞导致

## 8. 最终判断

本轮终验可以给出以下正式结论：

### 已通过

- 空库首次初始化
- 默认供应商 Base URL 归一化
- 后端自动化
- 前端自动化
- OpenAPI generated schema 同步
- 正式文档清理
- 用户手册补齐
- 正式前端入口可用
- EvalSuite / EvalCase 基础契约
- 空 `judgeRule` 创建允许、确认拒绝

### 未通过

- 真实 LLM 供应商连接
- 真实 DebugRun
- 真实 EvalRun 核心 judge 链路

### 未通过原因

- 当前 `12_test_env/test_env.md` 中提供的 Poe API Key 在正式容器内返回 `401 invalid_api_key`

## 9. 复验建议

当前不建议继续要求开发人员修改业务代码。

应先修复测试环境：

1. 替换 `12_test_env/test_env.md` 中失效的 Poe API Key。
2. 重新执行空库首次初始化：
   - `docker compose down -v --remove-orphans`
   - `docker compose up -d`
3. 先执行：
   - `POST /api/model-providers/1/test`
4. 连接测试通过后，再执行一次正式 DebugRun 和正式 EvalRun。
5. 重点复验：
   - 成功 NodeRun -> `AI_DRAFT_PENDING`
   - hardChecks 通过 -> judge 被调用
   - hardChecks 失败 -> judge 字段为 null
   - `judgeResult.score` 是唯一分数字段
   - `hardCheckResults` 为数组

## 10. 状态建议

建议将本变更状态更新为：

`真实验收已执行，因测试环境 Poe 凭据失效未通过，待环境修复后复验。`
