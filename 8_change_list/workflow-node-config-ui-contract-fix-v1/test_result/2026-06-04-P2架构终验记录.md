# 2026-06-04 P2 架构终验记录

## 1. 验收范围

本次终验只针对上一轮架构复验剩余并要求开发修复的 2 个 P2：

- 目录选择器和 Schema 选择器不得把“未加载详情 / 当前分页结果未命中”的已有绑定值误报为失效。
- `WorkflowPage.test.tsx` 必须补齐 JAVA_METHOD、TOOL、EXTERNAL_AGENT、输入 Schema、输出 Schema 的分页、保存和未修改保留路径测试。

## 2. 静态代码检查

检查文件：

- `11_code/frontend/src/features/workflow/pages/WorkflowPage.tsx`
- `11_code/frontend/src/features/workflow/pages/WorkflowPage.test.tsx`
- `8_change_list/workflow-node-config-ui-contract-fix-v1/*`

结论：

- `RemotePagedSelect` 使用 `page`、`pageSize=20`、`keyword` 调用分页接口，并通过加载下一页继续取数。
- Java 方法、工具、外部 Agent、Agent 调用和 Schema 选择器均通过 `boundValueFormatter` 回填当前绑定占位项。
- 当前实现的占位文案为 `当前绑定：...（未加载详情或当前结果未命中）`。
- 当前正式实现和测试中未发现继续使用 `已绑定失效项` 作为 UI 占位文案；历史验收记录中保留旧问题描述，不属于当前实现泄漏。

## 3. 自动化测试

执行：

```powershell
cd 11_code/frontend
npm test -- --run
```

结果：

- 8 个测试文件通过。
- 25 个测试通过。
- 0 失败。
- 测试输出存在 `Window's getComputedStyle() method: with pseudo-elements` 提示，属于当前测试环境能力提示，不是失败项。

覆盖确认：

- JAVA_METHOD：覆盖旧值保留、分页加载第二页、选择新值并保存。
- TOOL：覆盖旧值保留、分页加载第二页、选择新值并保存。
- EXTERNAL_AGENT：覆盖旧值保留、分页加载第二页、选择新值并保存。
- 输入 Schema：覆盖分页加载第二页、选择新值并保存为 `inputSchemaRef`。
- 输出 Schema：覆盖分页加载第二页、选择新值并保存为 `outputSchemaRef`。
- Schema 已绑定值：覆盖输入/输出 Schema 未修改保存时保留原引用。

## 4. 前端构建

执行：

```powershell
cd 11_code/frontend
npm run build
```

结果：

- `tsc -b` 通过。
- `vite build` 通过。
- 仍存在 Vite chunk 体积警告，不阻塞本次 P2 验收。

## 5. Docker 与 API 复验

执行：

```powershell
$env:AGENT_STUDIO_POSTGRES_PASSWORD='agent-studio-dev-pass'
$env:AGENT_STUDIO_OPENAI_API_KEY='local-acceptance-placeholder'
docker compose ps --format json
curl.exe -I http://127.0.0.1:18080/
curl.exe -I http://127.0.0.1:18080/agents/4/workflow
```

结果：

- Compose project 为 `agent-studio`。
- `agent-studio-postgres-1` 运行中且 healthy。
- `agent-studio-api-1` 运行中。
- `agent-studio-web-1` 运行中，宿主机端口为 `18080 -> 80`。
- `http://127.0.0.1:18080/` 返回 `200`。
- `http://127.0.0.1:18080/agents/4/workflow` 返回 `200`。
- `/api/ping` 返回成功，`serviceName=agent-studio-api`。
- `/v3/api-docs` 返回成功，`servers[0].url=http://127.0.0.1:18080`，`paths` 数量为 40。

读取 `http://127.0.0.1:18080/api/agents/4/workflow-draft` 的结果：

- `success=true`
- `workflowVersionId=9`
- `agentId=4`
- `versionNo=4`
- `status=DRAFT`
- `node-tool.name=Tool Node Accepted`
- `node-tool.config.toolKey=tool.sample.echo`
- `node-start.inputSchemaRef=java.sample.echo.input / v1`
- `node-start.outputSchemaRef=java.sample.echo.output / v1`

## 6. 页面只读复验

使用浏览器打开：

```text
http://127.0.0.1:18080/agents/4/workflow
```

只读操作：

- 选中 `Tool Node Accepted TOOL`。
- 选中 `Start Node START`。
- 未编辑字段，未点击保存。

页面结果：

- Tool 节点属性面板显示 `当前绑定：tool.sample.echo（未加载详情或当前结果未命中）`，出现次数为 1。
- Start 节点输入 Schema 显示 `当前绑定：java.sample.echo.input / v1（未加载详情或当前结果未命中）`，出现次数为 1。
- Start 节点输出 Schema 显示 `当前绑定：java.sample.echo.output / v1（未加载详情或当前结果未命中）`，出现次数为 1。
- 页面中 `已绑定失效项` 出现次数为 0。

## 7. 结论

本轮剩余 2 个 P2 已按预期闭合。

从当前代码、测试、构建、Docker 正式入口、API 数据和页面只读复验看，开发人员本轮修复符合架构预期：

- 没有继续把未加载详情误报为真实失效。
- 没有通过超大 `pageSize` 或只取第一页规避分页契约。
- 已补齐此前缺失的目录型选择器和 Schema 选择器测试覆盖。
- 未发现本轮 P2 修复引入新的双轨真相、兼容止血方案或不必要复杂度。

本次未跑真实 LLM 上游调用链；当前 Docker 复验使用的是本地占位 `AGENT_STUDIO_OPENAI_API_KEY`。这不影响本次 P2 验收结论，因为本次 P2 范围是工作流节点配置 UI 的选择器回填、分页、保存和页面展示契约。
