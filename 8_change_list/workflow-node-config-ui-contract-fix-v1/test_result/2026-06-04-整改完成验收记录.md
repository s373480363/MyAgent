# 2026-06-04 整改完成验收记录

## 验收结论

`workflow-node-config-ui-contract-fix-v1` 本轮已完成开发与验收收口。

本轮确认达成的正式契约：

- 工作流节点配置不再只有 JSON 入口，LLM / REVIEW / SUMMARY 已有显式表单字段。
- JAVA_METHOD / TOOL / AGENT_CALL / EXTERNAL_AGENT 已有目录选择器入口。
- START / END / JAVA_METHOD / TOOL 的 Schema 引用可以通过选择器配置。
- CONDITION 默认边新写入只保留 `edge.type="DEFAULT"`，历史 `isDefault=true` 只做读取归一化。
- 高级 JSON 入口保留，并与表单共享同一份节点/边对象。
- Agent 页面默认值文案已收口为 LLM 节点回退语义。

## 自动化验收

### 前端测试

命令：

```powershell
cd 11_code/frontend
npm test -- --run
```

结果：

- 8 个测试文件通过。
- 19 个测试通过。
- 新增覆盖：
  - LLM 节点显式表单保存。
  - 未知 `config` 字段保留。
  - 高级 JSON 与表单共享同一对象。
  - 历史 `isDefault=true` 归一化保存。
  - CONDITION 多默认边阻止保存。
  - AGENT_CALL 失效绑定值保留与分页过滤。
  - Agent 默认值文案。

### 前端构建

命令：

```powershell
cd 11_code/frontend
npm run build
```

结果：

- `tsc -b` 通过。
- `vite build` 通过。
- 存在 Vite chunk 过大警告，但不阻塞本次变更验收。

### 静态检索

执行结果：

- `rg -n "userPromptTemplate|systemPromptTemplate|temperature|model" 11_code/frontend/src/features/workflow`
  - 命中 `WorkflowPage.tsx` 的显式表单、配置读写与测试代码。
- `rg -n "baseUrl|apiKey|provider|supplier|AGENT_STUDIO_OPENAI_BASE_URL|AGENT_STUDIO_OPENAI_API_KEY" 11_code/frontend/src/features/workflow`
  - 无命中，确认供应商与密钥没有进入工作流节点配置 UI。
- `rg -n "节点属性|节点配置|LLM|CONDITION|userPromptTemplate|model|temperature|methodKey|toolKey|targetAgentKey|adapterKey|condition|未知 config|分页远程搜索|失效绑定|isDefault|高级 JSON" 4_arch_design 7_interface_design 8_change_list/workflow-node-config-ui-contract-fix-v1`
  - 设计文档、接口文档与本次变更文档对齐。

## 运行态验收

### 正式部署入口启动

命令：

```powershell
cd D:\myproject\MyAgent
$env:AGENT_STUDIO_POSTGRES_PASSWORD='agent_studio_dev_password'
$env:AGENT_STUDIO_OPENAI_API_KEY='manual-acceptance-key'
docker compose up -d --build
```

结果：

- `agent-studio-api`、`agent-studio-web`、`agent-studio-postgres` 启动成功。
- Web 正式入口 `http://127.0.0.1:18080` 可访问。

### 健康检查

命令：

```powershell
Invoke-RestMethod -Uri 'http://127.0.0.1:18080/actuator/health'
```

结果：

```json
{"status":"UP"}
```

### 页面可达性

使用 Edge headless 对正式入口做页面复验：

- `http://127.0.0.1:18080/agents`
  - 页面完成渲染。
  - DOM 中可见 `Agent 管理`、`创建 Agent`、Agent 列表行数据。

### 正式契约保存与后端校验

使用正式 REST 接口完成一条覆盖本次 UI 契约的工作流草稿保存与校验：

1. 创建验收 Agent：`agentKey=ui-contract-agent`
2. 保存工作流草稿，内容包含：
   - LLM 节点 `userPromptTemplate` / `systemPromptTemplate` / `model` / `temperature`
   - CONDITION 默认边 `type="DEFAULT"`
   - CONDITION 显式条件边 `type="CONDITION"` + `condition`
   - JAVA_METHOD `methodKey`
   - TOOL `toolKey`
   - START / END / JAVA_METHOD / TOOL 的 `SchemaRef`
3. 调用：

```powershell
POST /api/agents/2/workflow-draft/validate
```

结果：

```json
{
  "success": true,
  "data": {
    "valid": true,
    "errors": []
  },
  "error": null
}
```

说明：

- 这证明前端当前保存语义与后端发布校验契约一致。
- 也证明 `START/END` 的 Schema 选择器不是前端自作主张，而是后端真实要求。

## 本轮未引入的双轨

- 没有恢复旧的 `isDefault` 写入路径。
- 没有把目录选择器退回成首屏全量或超大 `pageSize` 拉取。
- 没有丢弃历史草稿中的未知 `config` 字段。
- 没有把高级 JSON 做成独立状态。
- 没有把 Agent 默认值文案继续保留为模糊的“系统提示词 / 默认模型 / 温度”。

## 备注

- `8_change_list/workflow-node-config-ui-contract-fix-v1/test_result/2026-06-04-完整验收记录.md` 为本次整改前的失败验收记录，保留作为历史证据。
- 本文件为整改完成后的最新验收结果，应作为当前事实来源。
